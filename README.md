# Titan Echo 2.0 · 泰坦遠征

以 Tap Titans 公開玩法為參考，自行重建的繁體中文點擊放置 RPG。不是官方遊戲、沒有反編譯或取得原版原始碼；角色、美術、數值與程式均為此專案重新製作，因此並非逐像素或完整內容的一比一移植。

## 2.0 新增內容

- 三種每日地下城、五項每日任務、連續簽到、24 項成就。
- 33 種英雄武器、全套加成、5 部位裝備、4 品質、分解與鍛造。
- 鑽石商店、英雄負傷與復甦、神器重鑄、個人週試煉、第二世界。
- 舊存檔自動升級並保留原有進度。
- 各項細節與原版差異見 [內容對照表](CONTENT-COVERAGE.md)。

## GitHub Pages

**[直接開始遊戲](https://zehoward.github.io/titan-echo/)** · [GitHub 原始碼](https://github.com/ZeHoward/titan-echo)

GitHub Pages 是公開靜態版：不需登入，遊戲每 3 秒自動儲存在此瀏覽器的 IndexedDB。重新整理與關閉後重開會恢復進度，並結算離線收益。右上角可以修改本機冒險者名稱，設定可匯出 JSON 紀錄。

Pages 不會執行後端程式，所以這個網址沒有多人帳號、跨裝置同步或全球排行榜；「本機紀錄」只顯示這個瀏覽器的進度。清除網站資料會刪除本機存檔。多人版本仍可使用下面的 SQLite 自架服務，程式及測試皆保留。

推送至 `main` 後，`.github/workflows/pages.yml` 會執行測試、檢查型別、建置並部署 GitHub Pages。網站子目錄由 Pages 設定自動取得，圖片、樣式及 JavaScript 都支援專案路徑。

手動產生 Pages 版時，設定 `VITE_PAGES_MODE=true`、`PAGES_BASE_PATH=/titan-echo/`，再執行 `npm run build:static`。未設定這兩個值時維持自架版的伺服器存檔行為。

## 直接遊玩（Windows 可攜版）

1. 解壓縮 `Titan-Echo-Web.zip`。
2. 雙擊 `START-GAME.cmd`，保持視窗開啟。
3. 用瀏覽器開啟 <http://localhost:4173>。
4. 點右上角帳號，輸入帳號與密碼，再按「建立帳號」。每位玩家建立不同帳號。

可攜版附 Node.js 24 執行環境，不需要安裝 npm 或申請 Google API。

同網路的其他電腦／手機開啟 `http://遊戲主機的區網IP:4173`，即可同時建立帳號、各自推關並共用排行榜。主機必須持續運作；Windows 防火牆需允許此連線。這是多人獨立進度，不是同一隻頭目的即時合作模式。

## 資料存在哪裡

自架版用 `data/game.sqlite`，單一 SQLite 資料檔取代外部資料庫服務。採用 WAL 與原子版本更新處理多人存檔。停止遊戲主機後，備份整個 `data` 目錄即可保留帳號、密碼雜湊、登入工作階段及所有玩家進度。請勿把這個目錄上傳到公開靜態空間。

正式網際網路架設請以 HTTPS 反向代理提供服務，設定 `COOKIE_SECURE=1`。可透過 `PORT`（預設 4173）、`HOST`（預設 0.0.0.0）及 `DATA_DIR` 調整監聽與資料目錄。這個自架版提供基本 scrypt 密碼保護、HttpOnly 工作階段與登入節流，沒有密碼找回或大型營運管理後台。

## 靜態網頁與雲端版

- `dist-static/` 是可直接上傳的靜態 HTML、CSS、JavaScript 和圖片。
- Pages 建置可以使用瀏覽器本機存檔；持久化多人帳號仍需要共用服務，不能僅靠每位玩家各自的瀏覽器檔案完成。
- 可攜版的 Node 服務負責提供靜態檔案與 `/api/*`，不必修改網頁。
- Sites 線上版使用 ChatGPT 登入與 Cloudflare D1，程式位於 `app/api/`。目前 Sites 發佈遵循擁有者私有權限；其他人須先獲得網站存取權限。自架版則可直接建立多個帳號。
- Sites 和自架版是不同存檔空間，不會互相同步。

## 玩法

- 點擊巨獸或按空白鍵攻擊；每 10 次點擊造成 5 倍暴擊。
- 每個普通關卡有 10 隻巨獸；每 5 關迎戰 30 秒頭目。
- 頭目逾時進入金幣農場，可手動重試。
- 33 位英雄自動攻擊，每 25 級傷害加倍、50 級提供團隊被動，100 級可進化；批量升級 1／10／25／100／MAX。
- 6 個技能：影分身、致命一擊、戰爭怒吼、狂戰之怒、黃金之手、天堂之擊。
- 50 關開啟轉生；30 件神器提供永久加成，6 種技能各有 10 級可強化。
- 60 種不同造型巨獸、10 幅不同環境；每 60 秒有仙女金幣。離線收益上限 8 小時，不離線推關。
- 自動存檔每 3 秒一次；關卡最高 1800，英雄／劍士最高 2000 級。
- 各帳號独立進度、共用前 50 名排行榜。排行榜每 10 秒更新。
- 設定中可匯出 JSON 紀錄供檢視；不允許匯入數值修改檔破壞排行榜。

訪客試玩不保存，也不會自動轉入登入帳號。網路中斷時會重試同步；同帳號多分頁競爭時載入較新版本，未儲存的舊分頁操作可能被放棄，因此建議每個帳號一次使用一個分頁。

## 開發

需要 Node.js 24+。

```sh
npm install
npm run dev
npm test
npx tsc --noEmit
npm run build:static
npm run start:standalone
npm run build
npm run db:generate
```

主要檔案：`lib/engine.ts` 共用戰鬥規則；`app/game.tsx` 遊戲介面；`server/standalone.mjs` 自架多人服務；`db/schema.ts` 雲端資料結構。原始圖片與提示詞說明見 `ASSETS.md`。

已驗證：36 項遊戲、存檔及整合測試，涵蓋 v1 遷移、神器、進化、地下城、週試煉、任務、裝備、多人隔離、IndexedDB 與衝突處理，以及 TypeScript 與正式建置。未做真人長時間平衡測試、大量同時在線壓力測試或瀏覽器互動自動化。

## 參考

- [Tap Titans 官方商店介紹](https://apps.apple.com/us/app/tap-titans/id940596201)：點擊、英雄、神器與轉生的原作功能。
- [公開遊戲說明](https://tap-titans.fandom.com/wiki/In-game_help)：30 秒頭目戰、農場模式與轉生循環。

本作重新設計成長数值與角色美術，完整實作與差異見 [CONTENT-COVERAGE.md](CONTENT-COVERAGE.md)。没有真實付費、連線錦標賽或好友聊天；個人週試煉不冒充全球排名。伺服器驗證操作與時間、限制點擊頻率，但不宣稱能阻止所有自動點擊工具。
