# Titan Echo 完整復刻代辦清單

更新：2026-09-17。遊戲目前為 **2.13.7**，最近完整測試 **421 項 Node 測試＋23 項 Python 匯入測試通過**。**核心公式來源登記表裡已經沒有任何 `baseline-75`**——67 項來源全部對過 8.2，不能照做的改列 `table-differs` 並寫明理由。此文件是後續工作的主清單；`TT2-RULES.md` 保留歷史研究紀錄，不以舊敘述判斷目前完成度。

## 目前位置

- **已完成 R01：固定目標、盤點差異、建立來源基準。** 產物：[來源與雜湊清單](docs/reference-baseline.json)、本清單與工作約定。
- **已完成 R02 的資料整備：8.2 全表匯入與完整 ID 目錄。** 133 張表、31,451 個有效 ID 的精確字串資料、七組舊 ID 對照、31,758 處引用檢查（errors 與 unresolved 皆為 0），372 個安裝包資源全部歸類完畢。**線上資料版本尚未切換**，遊戲執行時仍使用 7.5 數值；切換需要先完成 R04 的大數值處理，並逐項校正各系統公式。
- **已完成 R03：穩定 ID 存檔遷移、規則版本與還原備份。** 存檔槽與 TT2 區塊皆有 ID 對照與依 ID 搬移的遷移函式；舊 2.6 存檔只退款一次、重跑不變；8.2 新增的 6 項技能與 28 組套裝不會錯置；雲端與備份存檔可寬鬆讀取補齊後再嚴格檢查（遊戲 2.6.2）。**執行時資料版本仍為 7.5，切換屬 R04 之後的工作。**
- **已完成 R04：大數值與數字顯示。** 關卡、等級與金幣鏈已改用尾數／指數表示並改採安裝包記載的上限（關卡 98000、英雄 6000、劍術大師 12500），血量、傷害、金幣、費用在全程皆可運算與序列化（遊戲 2.8.0）。`lib/tt2-rules.ts` 的 `cap()` 仍把加成倍率夾在 1e240，但**這不是遊玩上的封頂**：把神器、天賦、寵物、卷軸、英雄等級與關卡全部推到規則允許的最高值後，640 個加成目標沒有任何一個接近該值，最大的 GoldAll 約 8.4×10^150，仍差 89 個數量級；該夾擠只在存檔帶著規則產不出的等級時用來避免 Infinity。
- 接續順序：R01–R04 已完成。**C01–C07 與 I01–I03 的「卡在證據」已經過時，2026-09-17 修正**：舊說法是「953 個 `[ServerVar]` 只有 28 個有值，這兩段的參數在包內全部沒有值」。那只對了變數表那一半——`ServerVarsModel` 的類別建構式還帶著**編譯期預設值**，953 個欄位裡 **769 個解得出來**，真正兩者皆無的只剩 183 個。分組來看：C05 泰坦與金幣 67 個中 61 個有預設值、I01–I03 裝備 26 個**全部**有、C01 暴擊 10 個中 8 個、C03–C04 英雄 22 個中 19 個、C07 魔力 18 個中 13 個（見 [伺服器變數缺口盤點](docs/server-var-gaps.md)）。**所以這兩段可以做了**，只是引用預設值時狀態是 `default`（線上可覆蓋），不能宣稱與線上一致；已經照這條路完成的有英雄升級費用（2.12.8）與每關小怪隻數（2.12.9）。**V 系列裡 V02 小遊戲同樣卡在證據**（六個模式的抽選結果都是伺服器回應），**V01 活動框架是半套**（貨幣賺取側在包內，獎勵路徑與時程來自伺服器），兩者都不要當成「還沒做」而重新開工；V03–V07 則按證據是否充足逐項推進。**已經查清楚、但要不要照做屬於取捨的十二項，集中在下一節「待決定的取捨」**；在使用者做出決定前不要自行改動那幾項，也不要當成沒查過而重查。
- 尚未有任何一個大型系統通過「與原版同版本完整一致」驗收。已有功能不會重寫成空白，但必須逐項校正與驗證。

## 待決定的取捨

這裡放**已經查清楚、但要不要照做屬於取捨**的項目：證據都在，缺的是一個決定。
不是「還沒查」（那在各階段的待證據欄），也不是「還沒做」（那在各階段的狀態欄）。
做出決定後，把該項移到完成紀錄並更新對應登記。

### 0. 要不要整組換成原生的數值曲線

- **現況**：怪物血量、怪物金幣、英雄傷害這三條曲線，原生的形狀與係數**現在全部都解出來了**。
  怪物側共用 `MonsterModel.GetMonsterBase`，形狀是
  `mult × base1^min(關卡, levelOff) × base2^(expo1 × max(關卡−levelOff,0)^expo2) ÷ base3^(…)`，
  十個係數的編譯期預設值是 mult 17.5、base1 1.38、base2／base3 10、levelOff 100、
  血量 expo1 0.03／expo2 1.098、金幣 expo1 0.04／expo2 1.036、transcendenceLevelOff 180000
  （高於關卡上限，除數在可玩範圍恆為 1）。英雄側則是**少一項** 1.035（見下一條）。
  引擎現用的是 7.5 近似：血量 `18 × 1.32^關卡`、金幣 `5 × 1.27^關卡`。
- **選項**：三條一起換／維持現狀／只換其中一條（不建議，它們是配套的）。
- **影響**（基礎值的常用對數，見 [monster-curve-evidence.json](reference/tt2/8.2.0/monster-curve-evidence.json)）：

  | 關卡 | 原生血量 | 引擎血量 | 原生金幣 | 引擎金幣 |
  |---|---|---|---|---|
  | 100 | 10^15.2 | 10^13.2 | 10^15.2 | 10^11.0 |
  | 250 | 10^22.6 | 10^31.3 | 10^22.4 | 10^26.5 |
  | 2000 | 10^134.7 | 10^242.3 | 10^115.0 | 10^208.2 |
  | 98000 | 10^9072.6 | 10^11817.4 | 10^5937.8 | 10^10173.4 |

  第 100 關以前原生較高，第 250 關以後原生遠低於引擎，而且血量與金幣同向下降。
  換掉等於整條進度曲線重來，既有存檔的關卡位置與金幣量會對應到完全不同的難度。
- 三條都登記為 `table-differs`，保留現值。

### 0.5 技能點要不要改用原生的發放節奏

- **現況**：引擎是「每 50 關一點、偏移 −1」（第 2000 關給 39 點）。原生的兩個 `[ServerVar]`
  編譯期預設值是 `skillPointsStageDelta` **500**、`skillPointsStageMin` **51**，
  也就是第 51 關起、每 500 關一點；原生另有一條走蛻變次數的發放線（`skillPointsPrestigeDelta` 50），
  本專案沒有實作。
- **選項**：照原生改／維持現狀／只補上蛻變那條線。
- **影響**：照關卡那條改，第 2000 關的技能點會從 39 點掉到個位數，66 個天賦幾乎全點不起來。
  但原生同時有蛻變線在發點，只改一條會比原版更苛刻。

### 1. 英雄每級成長率 1.035 要不要移除

- **現況**：引擎的 `heroDps` 在原生的三個因子之外，多乘了 `1.035^(等級−1)`。
  原生 `HelperInfo.GetRawDPS` 只有 `里程碑累計倍率 × 等級 × 基礎傷害`，
  兩個相關方法一個浮點常數都沒有載入，確定沒有逐級成長率
  （見 [hero-dps-evidence.json](reference/tt2/8.2.0/hero-dps-evidence.json)）。
- **選項**：照原生移除／保留現值。
- **影響**：移除後英雄傷害在 50 級少 5.4 倍、500 級少 2.9×10^7 倍、1000 級少 8.4×10^14 倍，
  現有存檔會直接卡關。而與它配套的怪物血量曲線（`18 × 1.32^關卡`）同樣是 7.5 近似，
  決定它的十個 `[ServerVar]` 在包內都沒有值，所以**沒有原版難度基準可以一起改**。
- 目前登記為 `table-differs`，保留現值。

### 2. 頭目倍率分段要不要套用

- **現況**：安裝包的 `ThemeMultiplierSequence` 分四段（1–5、6–39、40–59、60 起），
  引擎對所有關卡只用第一段，因此 6–59 關的頭目血量偏低。
- **選項**：套用分段／維持單段。
- **影響**：不只是換一組數字——原生選哪一段不是關卡的純函數
  （`GetScalingIndex` 從目前索引往前掃、蛻變後不退回），換段時還會套用該列的 bonusA／bonusB，
  而且整個序列是可被伺服器覆蓋的 `[ServerVar]`。照抄第一段以外的值會引入路徑相依的狀態。
- 目前登記為 `table-differs`，維持單段。

### 3. 103 個效果標籤要不要改用安裝包的官方繁中

- **現況**：103 個標籤都有對應的官方 `BONUS_` 繁中字串，其中 **90 個與現用譯法不同**
  （逐項對照見 [bonus-label-parity.json](reference/tt2/8.2.0/bonus-label-parity.json)，
  官方字串已備在 `lib/tt2-bonus-labels.ts`，尚未接介面）。
- **選項**：全面改用官方字串／維持現用譯法／逐項挑。
- **影響**：官方字串自己的用詞是矛盾的——「聖物／遺物」在 `BONUS_` 區偏遺物、其他字串偏聖物，
  「妖精／精靈」的方向相反。全面照抄會讓同一個東西在不同畫面有兩個名字。

### 4. 英雄昇階費用要不要改用六個逐階係數

- **現況**：引擎用等比近似；原生是 `HelperInfo` 的 `AscendCostExpo1–6` 六個逐階係數。
- **選項**：接上六個係數／維持等比近似。
- **影響**：接上也不能宣稱與線上一致——原生另有 `[ServerVar]` `ascendCostExpoScaling`、
  `maximumHeroAscension` 與 `ascensionMinGoldExp`，安裝包都沒有值。

### 5. 四個技能的次要效果要不要顯示

- **現況**：`ACTIVESKILLSECONDARY_*` 已備在 `lib/tt2-skill-text.ts` 的 `secondary` 欄，未接介面。
- **選項**：接上／繼續不顯示。
- **影響**：四句的 `{0}` 分別是機率或次數，格式各不相同，接錯會顯示錯的數字。

### 6. 外觀收藏要不要做成一份大半鎖住的清單

- **現況**：583 個頭像只有 74 個的解鎖條件算得出來（Stage 27、Milestones 19、PetLevel 15、
  EquipmentSet 10、Auto 3），其餘要賽事、活動、突襲或小遊戲；263 個稱號的解鎖類型
  全部是賽事／賽季／活動／Custom，在本專案沒有任何可達成途徑。頭像與外框還需要美術資源，
  安裝包素材不進版控。
- **選項**：只做那 74 個／做整份但標明不可解鎖／不做。
- **影響**：做整份會得到一份九成鎖住、且沒有圖的清單。

### 7. 寵物的改良段要不要改用原生的每 20 級

- **現況**：寵物加成的「改良乘數」段數，原生是
  `Min(Floor((等級 − petImprovementLevelStart) ÷ petImprovementLevelDelta), maxImprovementLevels)`，
  兩個 `[ServerVar]` 的編譯期預設值是 **100 與 20**；引擎用的是
  `max(0, floor((min(等級, maxImprovementLevels) − 100) ÷ 50))`。**三處不同**：每段的等級間隔
  20 對 50、`maxImprovementLevels`（表裡是 1600）原生夾的是**段數**而引擎夾的是**等級**、
  原生沒有把段數夾到 0 以上，所以等級低於 100 時乘數小於 1。
  寵物這條線的其餘部分都已核對相同：30 隻的資料逐格、三段傷害的 40／80 門檻、
  未出戰的每 5 級 5%、被動值繞著 identity 縮放（見
  [pet-growth-evidence.json](reference/tt2/8.2.0/pet-growth-evidence.json)）。
- **選項**：三處一起改／維持現狀／只改間隔不改 `maxImprovementLevels` 的角色（不建議，兩者同屬一條式子）。
- **影響**（以 `improvementBonus` = 1.5 的 21 隻為例，原生倍率相對引擎）：

  | 寵物等級 | 1 | 50 | 100 | 200 | 500 | 1000 | 1600 | 3000 |
  |---|---|---|---|---|---|---|---|---|
  | 引擎段數 | 0 | 0 | 0 | 2 | 8 | 18 | 30 | 30 |
  | 原生段數 | −5 | −3 | 0 | 5 | 20 | 45 | 75 | 145 |
  | 倍率 | 0.13 | 0.30 | 1 | 3.4 | 1.3×10² | 5.7×10⁴ | 8.4×10⁷ | 1.8×10²⁰ |

  早期變弱、後期暴增，等於整條寵物曲線重來；`improvementBonus` = 1.65 的 7 隻差距更大。
  這與第 0 條是同一類決定，差別在它只動寵物這一條線。

### 8. 蛻變門檻要不要隨歷史最高關卡上升

- **現況**：引擎的規則是「歷史最高到過第 60 關，之後任何時候都能蛻變」。原生不是固定值：
  `PrestigeModel.GetPrestigeStage` = `Math.Max(floor(prestigeMsPercentRequirement ×
  (基準關卡 − 進階起點) + 進階起點), minimumPrestigeStage)`，`CanPrestige` 只是拿目前關卡和它比 `>=`。
  兩個 `[ServerVar]` 的編譯期預設值是 **0.5** 與 **60**，基準取自 `maxPrestigeStageCount`
  （歷史最高蛻變關卡）——也就是**要推到歷史最高的一半才能再蛻變**。
  第 60 關那個下限與本專案沿用的數字相同（見
  [prestige-unlock-evidence.json](reference/tt2/8.2.0/prestige-unlock-evidence.json)），
  差的是它上面那一層。
- **選項**：照原生加上那層乘數／維持現狀／加上但係數放寬。
- **影響**：第一次蛻變前兩者完全一樣。之後會變成：歷史最高 1000 關的玩家，要再推到第 500 關才能蛻變，
  現在則是隨時可以。這不改任何數值，改的是「什麼時候可以蛻變」這條規則，會直接影響蛻變節奏與
  短循環刷聖物的玩法。原生另有兩個本專案沒有的前提——`GetAdvancedStartNextStage`（蛻變後的起始關卡，
  本專案一律回到第 1 關）與末日紀元的 `endgameMinPrestigeReqInc`（5000）——照做前要先決定它們怎麼算。

### 9. 蛻變聖物要不要改用原生的三段曲線

- **現況**：算式**已經完整還原**（先前只讀出三段中的兩段）。關卡先 `Min(關卡, relicsStageMax)`，然後：

  1. `relicStageMult2 × (relicStageOffset + 關卡)` — 1.5 ×(關卡 − 56)
  2. `relicStageMult1 × relicStageBase ^ (關卡 ^ relicStageExpo)` — 3 × 1.21^(關卡^0.48)
  3. `relicStageBase2 ^ (關卡 ^ Min(relicStageExpoMax3, relicStageExpo2 × (1 + relicStageMult3 × 關卡 ^ relicStageExpo3)))`
     — 1.002^(關卡^min(1.0155, 1.005×(1 + 5e-07×關卡^1.1)))

  三段相加後 `Max(0, ·)`。**十個係數全部有編譯期預設值**（先前記為八個）。
  引擎現用的是 7.5 近似 `max(1, floor(關卡^1.7 ÷ 100 × PrestigeRelic))`。
  完整對照見 [relic-curve-evidence.json](reference/tt2/8.2.0/relic-curve-evidence.json)。
- **選項**：整條換掉／維持現狀／只到某個關卡為止照原生（原生在低關卡本來就接近，這選項只是把發散點推後）。
- **影響**：

  | 關卡 | 60 | 250 | 1000 | 2000 | 5000 | 10000 | 30000 | 98000 |
  |---|---|---|---|---|---|---|---|---|
  | 引擎 | 10 | 119 | 1,258 | 4,090 | 19,419 | 63,095 | 408,419 | 3.06×10^6 |
  | 原生 | 19 | 337 | 1,996 | 7,526 | 324,452 | 1.02×10^10 | 3.48×10^30 | 4.15×10^101 |
  | 倍率 | 1.88 | 2.83 | 1.59 | 1.84 | 16.71 | 162,087 | 8.53×10^24 | 1.36×10^95 |

  第 2000 關以內同數量級，第 10000 關起第三段的指數頂到上限 1.0155，
  `1.002^(關卡^1.0155)` 開始主導，之後無上限地拉開。
- **與第 0 條配套**：原生聖物給得多，是因為原生的怪物血量在第 250 關之後也遠低於引擎——
  推得更遠、聖物更多是同一套設計。只換聖物這一邊會直接壞掉。
- **外層已經完全對齊**（2.13.4、2.13.5）：順序是 `Ceiling(曲線值 × PrestigeRelic
  × (1 + PrestigeRelicAdditive) × 累加倍率 × OnlyPrestigeRelic)`。前兩個乘數已接上，
  進位方向已改為 `ceil`，而累加倍率那一項展開後是 `1 + 0.00017 × Max(已擁有, 下一個門檻)`——
  本專案沒有那套系統，所以它恆為 1，省略它與原生等價。**這條待決定只剩曲線本體。**

### 10. 橫向手機要不要重新排版面

- **現況**：844×390（手機橫放）**沒有任何對應的樣式**——`app/globals.css` 的媒體查詢全部是
  寬度條件（1450／1120／780／380），沒有一條看高度，所以橫放時套用的是「平板以下的兩欄版」。
  實測 844×390：文件高 862、視窗高 390，**要捲兩頁多**。戰鬥區固定 690 像素高
  （`.battle-panel` 寫的是 `height:min(810px,calc(100vh - 153px))`，但同一條的 `min-height:690px`
  把它蓋掉了），怪物在 y=310 才開始，技能列在 y=677——**打怪的時候看不到技能列**。
- **問題在於空間真的不夠**，不是少一條覆寫。把固定高度全部壓到最小仍然不夠：
  視窗 390 減掉頁首與外距約剩 316，而戰鬥區內部光是 `stage-top` 48 ＋資源列 61 ＋血條 56
  ＋底部（傷害數值＋技能列）約 110 就吃掉 275，留給怪物只剩約 41 像素。
  再者 `.enemy-health{top:153px}` 與 `.pet-combat-status{bottom:182px}` 都是寫死的像素，
  而 `.monster-target` 是百分比，戰鬥區一縮這幾個就會互相重疊。
- **選項**：
  1. **不做**，維持現狀（橫放要捲動），文件註明本作以直向為主。
  2. **只把可見度往上推**：頁首 90→56、外距 26→10、`stage-top` 74→52，怪物提早約 72 像素露出，
     但技能列仍在視窗外。改動小、風險低，不解決根本問題。
  3. **橫向專用 HUD**：資源列與血條改成疊在戰鬥圖上的浮層，技能列改成右側直排，
     升級面板改成可收合的抽屜。做得完整，但等於替橫向重畫一套版面。
- 2026-09-17 點測記為 R-05，尚未改動任何樣式。

## 固定目標與完成標準

目標為 **Tap Titans 2 8.2.0** 安裝包可確認的玩法、內容、介面與行為。現行網頁使用 7.5 數值基準，加上部分 8.2 名稱與原生邏輯；這是過渡狀態，不能宣稱已完成 8.2。

安裝包不是官方線上狀態的完整備份。伺服器覆蓋、實驗分組、活動時程與配對規則分開記錄；未確認時標記「待證據」，不自行補成原版規則。公會及錦標賽以本專案玩家與自己的服務重建，不接入官方玩家資料。

架構維持 GitHub Pages 靜態前端，以 IndexedDB 保存遊玩進度，Google Apps Script／Sheets 保存少於 10 人的獨立帳號進度。新增共享公會、排行榜與賽事時，另實作共享狀態與伺服器驗證；不能把多人獨立存檔當作多人合作已完成。

一項機制要標記完成，必須同時具備：

1. 固定版本的來源與數值；公式不明處有明確待辦。
2. 實際戰鬥或資源事件已接上，涵蓋觸發、結束、上限與重置。
3. 介面顯示真實結果，繁中名稱與說明可理解。
4. 保存、讀取、跨裝置、蛻變及版本遷移保持正確。
5. 邊界案例、獨立數值範例與整合測試通過；時序或畫面相關功能另核對實際操作。
6. 如要宣稱「原版一致」，需同版本遊戲數值或操作紀錄對照。只有自己的測試通過，僅代表實作自洽。

狀態：**完成**＝符合該列驗收範圍；**部分**＝已有實作但未達完成條件；**待做**＝尚未實作；**待證據**＝缺少足以確定規則的材料。不是依清單列數計算復刻百分比。

## 第一階段：版本、數值與存檔基礎

| ID | 狀態 | 工作項目 | 驗收條件 |
|---|---|---|---|
| R01 | 完成 | 固定 8.2.0 目標，記錄安裝包與資料來源、目前版本混用情況 | 雜湊可重算，14 組主要資料表差異已盤點；不把預設值當成已知線上覆蓋值 |
| R02 | 完成 | 8.2 資料匯入與完整 ID 目錄 | 神器、法術、技能樹、裝備、套裝、英雄、寵物、被動等皆有版本與唯一 ID；處理新增／刪除／停用列及新增欄位；區分活動和常駐內容 |
| R03 | 完成 | 穩定 ID 存檔遷移、規則版本與還原備份 | 舊 2.6 存檔可遷移且重跑不重複退款；新增 6 項技能與 28 組套裝不錯置；雲端舊版資料有相容讀取流程 |
| R04 | 完成 | 大數值與數字顯示 | 取代 1e240 的實質封頂及未驗證的等級限制；巨大傷害、金幣、費用、聖物可運算與序列化，無 Infinity／NaN；雲端容量測試通過 |
| R05 | 完成 | 來源證據與同版本比對用例 | 每個核心公式記錄表、方法或實測來源；建立升級前後、施法前後、蛻變前後對照；缺少實測的項目保留待驗證標記。**登記表見 [docs/formula-sources.md](docs/formula-sources.md)**，58 項來源中 17 項仍為待驗證、7 項已知但引擎未照做 |

R02 子步驟：建立 8.2 全表索引 → 逐表 schema 解析 → 以 ID 映射至現行資料 → 保留版本差異與停用標記 → 產生逐 ID 待驗收清單 → 交給 R03 遷移。此階段不直接以新版陣列覆蓋線上存檔。**R02 標記完成的範圍只有資料目錄與逐 ID 核對**；資料齊備不等於任何玩法系統已還原，後續各項仍須逐條實作與驗證。

R03 子步驟：建立存檔槽與目錄 ID 的對照 → 以 ID 搬移進度並記錄增刪 → 接上實際遷移與備份 → 雲端舊版相容讀取 → 驗證重跑不重複退款。

**R03 本次完成第一步**：新增 [lib/tt2-save-ids.ts](lib/tt2-save-ids.ts)，把每個存檔欄位的每一格對應到目錄 ID。存檔的 heroes、weapons、evolutions、wounded 共 33 格對應 TT2_HEROES 前 33 個 ID，artifacts 與 artifactSpent 30 格對應 TT2_ARTIFACTS 前 30 個，skillLevels 6 格依既有顯示順序 [5,1,4,3,2,0] 對應 TT2_ACTIVE。這些對照以測試釘住執行時實際索引方式，資料一漂移就會失敗。

`migrateSaveArrays` 依 ID 搬移數值：目標沒有的 ID 連同其數值列入 dropped（不靜默丟棄），目標新增的 ID 從 0 開始列入 added，欄位格數改變預設拒絕（保護 Sheets 的 heroes[33]／artifacts[30] 外層格式），需明確選用才允許。因為是以 ID 為鍵，重跑同一次遷移結果不變，不會重複搬移或重複退款。

**已量化索引式遷移的風險**：33 個英雄槽與 30 個神器槽在 8.2 目錄中的位置恰好相同，但 **6 個技能槽有 4 個會錯位**（ShadowClone、HelperBoost、HandOfMidas、BurstDamage），因為存檔用的是顯示順序而非目錄順序。改以 ID 搬移後全部歸位。所有 69 個存檔槽的 ID 都能在 8.2 目錄找到對應，沒有孤兒。本次未改存檔格式、玩法、版本號，也未切換執行時資料版本。

**R03 第二步：修正讀取存檔時的重跑風險。** 原本的判斷是「規則版本相符且有 TT2 區塊」才視為已遷移，否則一律走舊版遷移路徑；而舊版路徑會退回神器投入、清空技能等級、擊殺數、世界進度與試煉。若雲端或備份回來的存檔規則版本正確但缺少 TT2 區塊（此區塊本來就分開保存），就會誤觸該路徑並清掉這些進度。改為只要規則版本相符就視為已遷移，缺少的 TT2 區塊直接補建，不再重跑遷移。

既有的不重複退款性質也已用測試釘住：舊 2.6 存檔遷移時把神器投入（前三件為等級平方）一次退回聖物，隨即把 artifacts 與 artifactSpent 歸零，因此重跑退款金額為 0。測試涵蓋首次遷移的退款金額與備份內容、重複讀取不變、TT2 區塊遺失時進度完整保留、TT2 區塊只寫一半時保留既有欄位，以及短陣列補齊後仍符合 Sheets 的 heroes[33]／artifacts[30] 格式。

**R03 第三步：雲端舊版資料相容讀取。** 原本讀取與還原都直接套用嚴格格式檢查，因此較舊版本寫入、欄位較短的雲端存檔會在補齊前就被判為格式錯誤而無法載入。現在讀取與還原改為先以寬鬆模式檢查（仍要求 version 2、欄位是陣列、數值有限、未超過容量），交給 hydrate 補齊成 heroes[33]／artifacts[30]／skillLevels[6]，再以嚴格模式確認一次；上傳與本機保存維持嚴格，不會寫出不合格式的存檔。測試涵蓋舊版短陣列可讀不可寫、補齊後前段進度保留，以及版本錯誤、非陣列、無限值、空名稱與超量存檔在寬鬆模式下仍被擋下。

**R03 第四步：TT2 區塊的 ID 對照與驗收條件測試。** TT2 區塊另有自己的定位陣列與索引清單：artifacts 與 spent 103 格對應全部神器、tree 66 格對應技能樹、petLevels 30 格對應寵物、scrolls 37 格對應英雄；已完成的套裝以 TT2_SETS 的索引存在 sets，套裝碎片的 set 欄位同樣是索引。這些都已納入 ID 對照，並提供 migrateIndexList 與 migrateSetPieces 依 ID 搬移索引清單。

**直接驗證驗收條件**：8.2 的技能樹比 7.5 多 6 項、套裝多 28 組。測試證明依 ID 搬移後，既有技能等級與已完成套裝全部落在正確位置，6 項新技能從未學習開始、不繼承他人等級；沿用舊索引則至少有一項會對到錯的技能或套裝。目標目錄移除某套裝時，該筆進度列入 dropped 並回報，其餘照常搬移；來源沒有的索引也會被回報而非保留。所有 TT2 槽位 ID 都能在 8.2 目錄解析。本次未改存檔格式、玩法或版本號。

**R03 收尾：還原備份已逐項有測試。** 舊版遷移會先寫入 before-tt2-migration 備份且版本號只加一次；雲端還原會寫入 before-cloud-restore 備份、原子地換版本號，並在版本號過期時拒絕覆蓋。新增端到端測試：以舊版短陣列的雲端存檔還原，欄位會補齊為 heroes[33]／artifacts[30]／skillLevels[6]，前段進度與最高關卡保留，TT2 區塊重建，本機備份仍為還原前的內容。R03 的三項驗收條件（重跑不重複退款、6 項技能與 28 組套裝不錯置、雲端舊版相容讀取）皆已有可重跑的測試證據。

R02 本次完成：[獨立參考目錄](reference/tt2/8.2.0/README.md)、372 個資源雜湊索引、50 張表 schema 與逐 ID 待驗收記錄、七組 2.6 陣列的穩定 ID 映射、版本增刪與欄位差異。數值以原始十進位字串保存，避免溢位。累計 30 項 Node 資料驗證測試。未改遊戲執行資料、存檔或版本號。

本次追加：成就、每日任務、寵物任務、泰坦研究／卡片／升級、寶石研究、活動設定及挑戰賽設定共 9 張表。跨表引用檢查沒有未知 ID；9 處僅能在原生 BonusType 列舉確認，公式仍待驗證。逐 ID 分類識別 3 種妖精效果、56 組活動套裝、332 筆活動或限時裝備，以及明確停用的每日任務 UniqueArtifacts；不把資料表旗標當成線上活動時程。

本次已解決裝備副屬性重複鍵：原生解析器按列遞增，五個欄位解析成功後以字典 set_Item／OverwriteExisting 覆寫；AllActiveSkillAmount 採最後有效列的 PowerExp 1.002。59 列整理為 58 個 ID，保留舊值 0.4、來源列號及解析器雜湊證據。其他表維持拒絕重複 ID，新增 6 項 Python 匯入測試並接入 CI。

本次完成突襲核心 5 表：44 張卡片、150 級費用、3,000 級玩家資料、10 種敵人、66 筆部位定義（含 None 哨兵）。驗證 A／B 等級欄位、費用對應、部位保護／左右／連動引用及 A 至 H 效果 ID；保留負值效果，不把資料上限當成已確認的線上上限。全目錄目前 12 處效果只由原生列舉確認 ID，公式仍待驗證。玩家禮包引用已於後續商店資料匯入完成核對。

本次追加 639 筆禮包、140 個突襲關卡及 7 個區域；關卡以 TierID＋LevelID 組成唯一鍵，驗證敵人池、區域、禮包串接、裝備清單與卡片 ID／數量。RaidEnemyLayout 已確認為圖集 JSON，單獨記錄 65 個部位 ID，不作關卡 CSV 匯入。所有已納入檢查的引用均能解析；原有 513 個配送／複合獎勵欄位已完成下一批格式盤點，詳見下段；不代表可直接發獎。

本次完成 510 個複合獎勵欄位的來源格式解析與 ID 核對，另匯入原生 RewardID 列舉作為類型證據。數量保留字串，選擇槽候選、公會贈禮及一般獎勵分開記錄；當時未推定每槽選取數或實際發放對象，後續原生核對見下段。只剩 3 個 DailyDeliveryID 列為缺少配送表，客戶端 DailyDeliveryModel 有商店回應解析方法，不能用假資料補成真實配送規則。格式通過不等於發獎邏輯已還原。

本次已核對 RewardClass 與 ParsedShopBundleInfo 原生解析：一般獎勵以逗號／分號分隔，選擇槽以逗號分候選、候選內可含分號多獎勵；Equipment:Rare／Legendary 合成獨立 RewardID。ShopModel 每槽保存一個索引，未選取為 -1。新增可重跑的 12 處指令／3 個字串核對工具，保留方法雜湊。510 個來源欄位已重新解析；套裝 ID 仍不推定數量，購買、發獎與配送規則尚未完成。

本次追加 11 張寶石／終局表，來源變體分開保存，未以 _1 版覆蓋同名 ID。寶石成本 5,000 級、抽取權重 1,000 級；保留原始權重及百分比註記，不推定超出表範圍的行為。寵物任務 120 級的 10 維難度機率向量已檢查。新增 [剩餘資源盤點](docs/reference-coverage.md)：50 表已匯入、1 圖集已索引，另 321 個 TextAsset 尚未匯入，其中包含非資料表；不作完成率計算。

本次追加頭像 583 筆、外框 65 筆、稱號 263 筆，完成兩組季節合計 8 筆排名獎勵 ID 核對。Title:634 的值是稱號 ID，不是數量；不因稱號目錄另有第一名稱號就自行加到獎勵中。第 12／13 季來源分開保存，線上啟用條件未知。全目錄仍有 3 個每日配送 ID 缺少實際配送表。

本次完成外觀支援資料：AvatarParticleInfo 22 筆、ProfileBackgroundInfo 47 筆，並匯入 native-cosmetic-types.json（AvatarID 608、AvatarFrameId 66、AvatarParticleID 24、AvatarUnlockType 201、ProfileBackgroundType 3）與 5 個原生解析／取用方法位址，作為欄位型別證據。驗證器改以原生列舉核對五張外觀表的 ID、解鎖類型與背景分槽，新增 1,650 處引用檢查全部可解析，總數 17,531 增為 19,181。

四張外觀表的解鎖欄位都由同一個原生 AvatarUnlockType 型別化，201 個成員涵蓋全部來源值；PlayerTitleInfo 的 TitleID 原生是 int、ProfileBackgroundInfo 的 ID 原生是字串，沒有 ID 列舉可核對，已在 tableTypes 記錄原因。列舉中沒有資料列的成員只列在 cosmeticCoverage.enumOnly（AvatarID 25 個、AvatarFrameId 僅 None、AvatarParticleID 為 None 與 Particle1），不補進目錄。

ProfileBackgroundInfo 與 7.5 快照逐格相同；AvatarParticleInfo 沒有 7.5 對應檔，differenceFrom75 保持 null，不推定它必為 8.2 新增。背景表只有 Player 14 筆、Raid 33 筆的分槽欄位，表內沒有解鎖欄位，解鎖來源待證據。原生 RewardID 雖有 AvatarParticle、ProfileBackground、ProfileBackgroundPlayer、ProfileBackgroundRaid，但目前所有來源獎勵欄位都未使用，解析器維持拒絕這四種代號，不預先實作發放。未改遊戲執行資料、存檔或版本號。

本次追加伺服器變數與來源變體 6 張表：ServerVarsInfo 12 筆、ServerVarOverride 25 筆（來源 26 列）、ArtifactCostInfo_A 194 筆、TitanScalingInfo_A／B／C 共 21 筆。新增 `tools/audit-servervars-parser.py` 與 servervars-parser-evidence.json，核對 28 處指令、14 個方法名稱及 8 個字串引用。**兩張伺服器變數表都是安裝包內附值**，分類 bundled-server-variable、liveAvailability=unknown；maxStage 98000、relicsStageMax 180000 等數字是資料事實，不得當成線上上限直接改寫 R04／C05。

ServerVarOverride 的重複鍵依原生證據解除隔離：載入器按遞增列號讀取，跳過空白鍵與空值後呼叫 Dictionary.set_Item，故最後成功解析的列生效。pet_paradise_shovel_purchase_daily_limit 在第 14 與第 18 列（列號從 0 起）出現兩次、值皆為 3，26 列整理為 25 個 ID，兩列都保留在 duplicateHistory。此政策只適用本表。

泰坦縮放確認為 A／B 測試來源：ParseTitanScalingInfo 呼叫 GetABTestInfoDoc("TitanScalingInfo")，執行期取得的 A／B 表名才決定實際使用哪一份，該表名不在安裝包內。新增 variantOfSource 與 sourceVariants 逐 ID 比對七組變體：ArtifactCostInfo_A、TitanScalingInfo_C 與基準表完全相同；_A 多關卡 105／110／120／200／500；_B 多關卡 2 至 5 且關卡 1 的 BonusAmountA 由 1.00E+00 變 3.00E-01；三組終局 _1 只有欄位值差異。liveSelection 一律 unknown，不選用任何一組。

ScheduledBonusInfo 不匯入，記入 quarantine.json：原生沒有此表的 InfoDoc 逐列解析器，改由 ParseAllBonuses 與 TryParse 從伺服器字典建立；安裝包 4 列中只有 1 列有 BonusType，其餘 3 列只有註解。未改遊戲執行資料、存檔或版本號。

本次追加突襲進度 13 張表：大師階級關卡 190 筆（TierID＋LevelID 複合鍵）、單人突襲關卡 1,200 筆（WorldID＋LevelID）、農場關卡 120 筆、研究 94 筆、卡片等級獎勵 150 筆、票券加成 301 筆、忠誠 13 筆、加成槽 14 筆、敵人附魔 3 筆、兩張快速完成加成表 9 筆，以及大師階級排名獎勵與其 _1 變體各 5 筆。引用檢查由 19,202 增為 30,124，errors 與 unresolved 皆為 0。

新增 `tools/audit-infodoc-parser.py` 與 infodoc-parser-evidence.json：InfoDocs.ParseInfoDoc 以 set_Item 建立欄位字典，**重複欄位名以最後一欄索引生效**。RaidTicketBoostInfo 的 Amount5 與 RaidResearchInfo 的 Total Levels 各重複一次，只對這兩張列名表放行，並在 columnResolution 保留來源索引、生效索引與被遮蔽欄位的逐列原值，不丟任何儲存格。其他表出現重複欄位仍直接停止匯入。

**發現版本落差**：大師階級排名獎勵引用的 AvatarClanMasterTierSeason17Top10／25／50／100 既不在 AvatarInfo 也不在原生 AvatarID 列舉中，8 筆記入 deferredReferences，不自行建立頭像。判定規則為「目標表有 ID 列舉且該 ID 也不在列舉中」才算來源缺口，否則仍視為匯入遺漏並報錯。另有 17 列突襲研究的前置指向不存在的 999，這些列 BonusType 為 None、MaxLevel 與成本皆為 0，屬無效果列才予以延後；若出現在有效果的列上仍會使驗證失敗。

獎勵解析新增實際出現的 FortuneRaidCard 與 PlayerRaidXP 純量代號及 Reward／NewPlayerReward 欄位；AvatarReward 為 AvatarID 參照而非獎勵字串。突襲戰鬥、傷害公式、票券消耗與發獎仍未實作。未改遊戲執行資料、存檔或版本號。

本次追加錦標賽 7 張表：一般與超級錦標賽獎勵 273／297 筆（PrizeID＋TierID＋StartRank＋PrizeType 複合鍵）、新手錦標賽 5 筆、挑戰賽與超級挑戰賽獎勵各 30 筆、挑戰賽進度獎勵 196 筆、挑戰賽神器池 103 筆。引用檢查 30,227 處，errors 與 unresolved 皆為 0。

**錦標賽獎勵字串不是原生 RewardID 文法**：14 種代號中 FortuneHelperWeapon 與 RandomLevelPet 不在 RewardID 列舉內，只以原生字串存在。新增 tools/tournament-reference.mjs 專責切分這些欄位並記錄 nativeRewardId（無對應時為 null），不送進 RewardID 解析器、不改名、不推定發放。

獎勵字串與同列數值欄位的一致性已逐列比對：一般錦標賽、挑戰賽與超級挑戰賽三張表零分歧；**SuperTournamentRewardInfo 有 198 列分歧**（Perk 欄 99 處、PremiumWeapon 欄 198 處），兩邊都保留不判定對錯。挑戰賽神器池的 A 至 E 是權重而非旗標，Artifact20／29／35 在五個池權重皆為 0，已逐池統計記錄。未改遊戲執行資料、存檔或版本號。

本次追加 ChallengeTournamentStartingInfo 16 筆與 native-server-var-fields.json（953 個原生 [ServerVar] 欄位名）。此表的 BonusTypeA 至 I 是「BonusID:數值」成對字串而非裸 ID，已排除在通用 BonusType 規則外並改為拆對核對：九個欄位串接必須等於 StartingBonusTypes、ID 必須在 BonusInfo、數值必須合法，16 筆全數相符。StartingPassiveLevels 的八個名稱與同名欄位數值也全數一致。DiscoveryPool 對應神器池欄位，裝備與寵物代號全部可解析。引用檢查 30,883 處，errors 與 unresolved 皆為 0。

**伺服器變數鍵綁定已核對**：37 個鍵中 28 個對得上原生 [ServerVar] 欄位，9 個對不上（ServerVarsInfo 的 useChestSpecial 等 7 個、ServerVarOverride 的 EquipmentLevelReductionMin 與 EquipmentLevelReductionAmount）。這些鍵在此版客戶端沒有可綁定欄位，SetVarsFromAttributes 無法套用，記入 validation.json 的 serverVarBinding，資料列保留不刪。未改遊戲執行資料、存檔或版本號。

本次追加商店與寵物樂園 4 張表：ShopDisplayInfo 18 筆、AdChestInfo 83 筆（四欄複合鍵）、NewPrizeInfoDoc 273 筆、PetParadiseLevelInfo 2,011 筆。廣告寶箱的 RewardCategoryTier 全為原生 RewardID、ChanceTier 皆在 0 到 1；寵物樂園逐列檢查 Rows×Columns 等於 BoardSize 且偏好食物種類不超過生成種類，2,011 筆全通過。引用檢查 30,966 處，errors 與 unresolved 皆為 0。

NewPrizeInfoDoc 與 TournamentRewardInfo 的 273 個複合鍵完全相同，納入來源變體比較；變體報告新增 sharedColumns／variantOnlyColumns／baseOnlyColumns，並修正為只比對雙方保留欄位，不再把被 omit 的名稱欄算成差異。

四個商店 JSON 判定為**商店回應測試樣本而非資料表**，改以 shop-payload-samples.json 只記錄形狀：ShopInfo 不是嚴格 JSON（第 26 行多餘逗號），其餘三個頂層為 section_chests／section_daily_deals／section_special_bundle，product_type 含 daily_deal。含 uuid 與 expire_time 的樣本不能當線上商品、價格或每日配送來源，ShopBundleInfo 的 3 個 DailyDeliveryID 缺口維持不變。未改遊戲執行資料、存檔或版本號。

本次追加小遊戲與周年賽 24 張表（落球、挖掘、釣魚、狩獵、迷宮、公會保險庫、蛻變賽、周年錦標賽），含挖掘關卡 1,000 筆、魚類與野獸各 50 筆、飾品 43 筆、活動任務 15 筆及各遊戲的排名、貢獻、升級與稀有度表。三張升級表的 21 個 BonusType 全部是真實 BonusInfo ID。引用檢查 30,987 處，errors 與 unresolved 皆為 0。

**獎勵解析新增 RewardedBonus 三段形式**（BonusID 加小數比例），記為 itemId 與 amount 而非數量，50 處挖掘關卡獎勵使用；另補上 AnniversaryMinigameCurrency、MinigameMazeKeys 純量代號與 RewardStringForRarity4 欄位。小遊戲 1,334 筆獎勵引用全部以原生 RewardID 文法解析成功。MinigameEventQuestInfo 的 Requirement 與 Reward 是成對數字階段清單，改用成就式配對檢查，不當獎勵字串。

**再發現一處來源缺口**：周年錦標賽排名獎勵引用的 AvatarAnniversary10 不在 AvatarInfo 也不在原生列舉中，2 筆記入 deferredReferences；連同大師階級第 17 季的 8 筆共 10 筆。未改遊戲執行資料、存檔或版本號。

本次追加節慶與全球活動 10 張表：炸彈遊戲關卡 1,300 筆、全球突襲目標區 1,199 筆（三欄複合鍵）、單人貢獻 101 筆、全球突襲關卡與獎勵各 45 筆、部位摧毀順序 16 筆、活動貨幣 8 筆、全球任務 3 筆與活動設定 1 筆。全球突襲關卡的 AreaID 是逗號清單，已改用多值解析。引用檢查 31,357 處，errors 與 unresolved 皆為 0。

**節慶全球突襲頭目有獨立的部位名稱**（Head、Chest、LeftArm 等 8 個），全部不在 RaidEnemyPartInfo，記入 holidayRaidParts 而不強行對應；同活動的部位摧毀順序則確實使用突襲部位 ID。

獎勵詞彙模組改名 sheet-reward-reference.mjs：非 RewardID 詞彙不只在錦標賽，炸彈遊戲關卡獎勵也用 FortuneHelperWeapon，該欄改走表單詞彙記錄。目前 17 種表單代號中 FortuneHelperWeapon（494 處、4 張表）與 RandomLevelPet（96 處）無原生 RewardID。其餘活動獎勵 149 筆仍走原生文法，並補上 HelperWeapon、Perk、HolidayCurrency 純量代號。未改遊戲執行資料、存檔或版本號。

本次從需人工判別的 227 個資源挑出 16 張真正的資料表匯入：泰坦召喚等級成本 3,000 筆、召喚卡片機率 1,000 筆、妖精獎勵表 256 筆、背景 70 筆與循環 48 筆、影片妖精出現 56 筆、貼圖 27 筆、教學事件 51 筆與其 _A／_B 變體、支援選項 11 筆、建構指南 9 筆、QTE 9 筆、賽季排名 5 筆、面板變體 4 筆。其餘不是資料表：約 86 份 AnimationFlags 與 14 份 TrackFlags 是無標題動畫時序、Font*Asian 是 BMFont 描述、LineBreaking_* 是斷行字元清單。引用檢查 31,758 處，errors 與 unresolved 皆為 0。

匯入器新增「鍵欄位永不省略」規則（SupportInfo 以 Description 為鍵），並把 StickerImage 併入圖片省略清單。新增 native-fairy-reward-types.json（FairyReward 列舉 24 個成員），影片妖精的 8 種 FairyID 全部對上。QTE 天賦與冷卻加成、賽季排名的徽章與 14 組成對加成、建構指南的 165 件裝備／45 個天賦／75 件神器、召喚成本的商店禮包、召喚旗幟對泰坦卡 SubType 等引用全部解析。教學事件 Objective 僅接受四種形式。

TutorialEventInfo 的 _A 有 27 列與基準不同、_B 與基準完全相同，兩份都保留不去重。未改遊戲執行資料、存檔或版本號。

本次完成在地化盤點與資源歸類。16 個語言檔是 JSON 鍵值字典、LocalizationInfoLoadingScene 是逐語言欄位 CSV，全部只索引鍵不複製譯文（localization-index.json）。英文 13,122 鍵，其餘 15 種語言各 13,114 鍵、無多餘鍵、各缺同樣 8 個英文鍵。**繁體中文缺口**：8 個鍵不存在、2 個鍵為空、178 個鍵仍是英文原文，這份逐鍵清單即為 V05 的待辦來源。

剩餘資源盤點改為依實際位元組判定格式：214 個非資料表細分為二進位 91、無標題定位列 103、JSON 文件 13、BMFont 4、單行字元清單 3。quarantine 的不匯入清單增為 3 筆（ScheduledBonusInfo、UpdateInfo 156 個版本說明、EquipmentTest 6 列測試夾具）。**372 個資源已全部歸類，「尚未處理」為 0**，R02 的資料整備到此結束。未改遊戲執行資料、存檔或版本號。

**下一項：R03 穩定 ID 存檔遷移。** 依 legacy-2.6.json 的七組陣列對照，把舊 2.6 存檔遷移到 8.2 ID；驗收條件為重跑不重複退款、新增 6 項技能與 28 組套裝不錯置、雲端舊版資料有相容讀取流程，且 Sheets 外層格式 version 2／heroes[33]／artifacts[30] 維持可讀。線上活動時程、每日配送、A／B 指派與伺服器覆蓋值仍為待外部證據，不因資料齊備而推定。

R04 子步驟：找出溢位與非有限值 → 以極大存檔釘住不變量 → 蒐集原生數值型別與伺服器上限證據 → 取代 1e240 的自訂封頂 → 數字顯示與雲端容量驗收。

**R04 本次完成第一步：修掉真實的溢位缺陷。** `stateEffect` 的加算分支未封頂，因此加成堆疊到上限時，魔力上限、魔力回復、技能持續與冷卻會算出 Infinity，並寫進存檔；而存檔驗證會拒絕非有限值，導致本機保存與雲端同步雙雙失敗。已在 `stateEffect` 兩個分支都收斂，並發現兩個已封頂值相乘仍會溢位，故組合後的魔力回復再次收斂。

新增極大存檔測試：把金幣、聖物、鑽石、粉塵設到封頂，英雄、神器、技能樹、寵物與卷軸設到極高，驗證離線 30 天與一年、以及點擊、升級、施技、買神器、製作、蛻變各動作後都沒有非有限值；魔力上限與回復維持有限且不超過封頂；序列化後仍通過雲端格式檢查且小於 40,000 位元組，來回轉換不遺失封頂值。

**R04 第二步：數字顯示與原生數值型別證據。** 顯示函式在收到非有限值時會印出「NaN×10^NaN」，已改為 NaN 與負無限顯示 0、正無限顯示上限；中文單位由萬到載依序對應每 10^4 一階，超過載改用科學記號，已逐點測試。

新增 [native-number-type.json](reference/tt2/8.2.0/native-number-type.json)：原生 `GHDouble` 是**尾數與指數分開存的結構**，並定義 MaxValue、PositiveInfinity、NaN 等常數，**沒有 1e240 這種等級的上限**；因此網頁端的 1e240 是自訂安全值，不是原版規則。同時記錄哪些上限以 [ServerVar] 形式存在：maxGold 有欄位但安裝包沒有值（線上上限未知），maxStage 98000（三平台相同）、relicsStageMax 180000、skillPointsPrestigeMax 180000、additiveRelicMultiplierMax 150000 有包內值但非線上生效值。

**R04 第三步：尾數／指數運算型別。** 新增 [lib/big-number.ts](lib/big-number.ts)，以「尾數乘 10 的指數次方」保存數量級，與原生 GHDouble 的兩個部分對應。提供正規化、加減乘除、次方、比較與儲存字串來回轉換；次方在對數空間計算，指數不會溢位；超出 double 範圍時轉回一般數字會飽和到 MAX_VALUE 而不是 Infinity；非有限輸入、除以零、負數的非整數次方與無法解析的字串都會直接失敗，不靜默處理。

測試涵蓋 1e400 × 2e400 = 2e800 這類 double 無法保存的運算、差距超過雙精度時較小項不影響結果、跨正負與跨數量級的排序，以及儲存字串逐字來回一致。

**R04 第四步：量出封頂的實際影響。** 以現行公式量測，怪物血量在**關卡 1982**、金幣獎勵在**關卡 2307** 就會黏在 1e240；而引擎另有 `Math.min(1800, stage+1)` 的硬上限，所以玩家實際碰到的是 1800 關。1800 關的血量距封頂只剩約 21.8 個數量級，也就是**單獨移除 1800 關硬上限只能多推約 180 關**，之後血量與金幣就會完全持平、戰鬥與經濟失真。

**這把 C05 的「移除 1800 關硬上限」與 R04 的數值表示法綁在一起**：安裝包記錄的 maxStage 是 98,000，目前可達的 1800 關只有其 2%。兩個限制必須一起處理，先換數值表示法才有意義。已加測試釘住 1982／2307／1800 這三個數字，任何改善都會讓測試明確變動而不是悄悄通過。

**R04 第五步：未驗證的等級限制已逐項對照證據。** 引擎有三個硬編上限，全部與安裝包資料矛盾：關卡 `Math.min(1800, …)` 對上 maxStage 98,000；英雄與劍術大師等級都寫死 2000，但 HelperImprovementsInfo 的英雄里程碑到 **6,000 級**、PlayerImprovementsInfo 的劍士里程碑到 **12,500 級**。

**但放寬上限同樣被數值上限擋住**：量測顯示英雄 DPS 在 **4,999 級**、劍士裸傷在 **7,899 級**就會觸及 1e240，都達不到資料記載的 6,000 與 12,500。因此這三個上限不能單獨調高，必須與數值表示法一起處理。已加測試釘住 1800／2000／2000 這三個自訂值、資料記載的 98,000／6,000／12,500，以及 4,999／7,899 的實測餘裕。

**尚未接入引擎**：接入會改變存檔中數值的表示法（金幣與目前怪物血量是關鍵欄位，聖物在 18 萬關也只到 8.6e6，不受影響），必須一併處理 Sheets 外層格式相容、雲端容量與既有存檔遷移；且在取得 maxGold 的線上值之前，仍無法宣稱與原版同上限。這是 R04 剩餘的工作。
## 伺服器參數的證據界線

原生把大量玩法參數放在 `[ServerVar]` 靜態欄位，共 **953 個**。這裡有**兩條各自獨立的線索**，先前只記了第一條：

1. **安裝包的兩張變數表**只帶 37 個鍵，其中 28 個對得上原生欄位。依 ROADMAP 項目分組後
   （見 [伺服器變數缺口盤點](docs/server-var-gaps.md)），第二階段各項的參數在這兩張表裡全部沒有值：
   C01 暴擊 10 個欄位 0 個有值、C02 劍術大師 4 個 0 個、C03-C04 英雄 22 個 0 個、C05 泰坦與金幣 67 個 0 個、
   C06 濺射與跳關 3 個 0 個、C07 魔力 18 個 0 個、I01-I03 裝備 26 個 0 個。
2. **程式裡寫死的編譯期預設值**：`ServerVarsModel` 的類別建構式會在任何伺服器回應之前先賦值。
   `tools/audit-servervar-defaults.py` 從中解出 **954 個靜態欄位裡的 770 個預設值**
   （見 [靜態預設值總表](reference/tt2/8.2.0/servervar-defaults.json)），其餘 184 個逐項列名：
   string 39、bool 63、int 46、int[] 12、float 14、double 5、TimeSpan 2、GHDouble 2、List&lt;float&gt; 1。
   GHDouble 欄位另外處理：它是以 `GHDouble..ctor(double)` 建在堆疊上再複製進靜態區塊的，
   解出來的是**建構時的那個 double**，怪物血量與金幣曲線的八個係數就是這樣拿到的。
   劍術大師的 `costBase`／`costGrowth` 與英雄的 `helperUpgradeBase` 本來就是從這裡取得的，
   三者同時作為這份解析的自我檢查。

**兩條線索互證，也彼此區別**：`hoursToCollectEgg` 的預設值 4 與變數表的 4 一致；
`clanNameChangeCost` 兩邊都是 800。而 `maxStage` 的編譯期預設值是 **1,000,000**、被變數表覆寫成 **98,000**
——這正說明**預設值不是線上值**：每一個都是 `[ServerVar]`，伺服器可以整份覆蓋。
因此引用預設值時狀態是 `default`（原生靜態預設值，線上可覆蓋），不是 `native`。

**這界定了「原版一致」在本專案能到什麼程度**：資料表（曲線、費用、效果 ID）可以從安裝包核實，
編譯期預設值可以作為 `default` 級的對照，但線上實際生效的參數仍然不在包內。
因此第二階段各項在取得線上參數或實測紀錄之前，只能做到「依資料表、原生程式結構與編譯期預設值還原」，
不能宣稱數值與線上一致。盤點由 `node tools/server-var-gaps.mjs` 產生，可重跑。

**小遊戲是同一類界線，但原因不同**：不是參數缺值，而是**結果本身由伺服器決定**。六個小遊戲模型都把花費送出、再從回應解析結果——釣魚 `PullFishFromServer`／`TryParsePulledFish`、狩獵 `PullBeasts`／`TryParsePulledBeasts`、挖掘 `RevealTile`（翻一格就是一次往返）、落球 `Submit`、迷宮 `OpenDoor`、公會保險庫 `CollectRewards`。連落球的釘子數值（`ParsePegsAndBuckets`）與迷宮地圖（`ParseMaze`）都是從伺服器字典解析的。因此安裝包內那些魚、野獸、關卡與稀有度機率表是**顯示用的資料**，不是客戶端的抽選規則；由 `python tools/audit-minigame-boundary.py` 對釘住的程式庫重新產生，逐方法位址與位元組雜湊見上述證據檔。

## 第二階段：核心戰鬥與升級

| ID | 狀態 | 工作項目 | 驗收條件 |
|---|---|---|---|
| C01 | 部分・待證據 | 普通點擊、暴擊與致命爆擊 | 核對一般／暴擊／致命傷害順序、機率、倍率、上限與流派差異；移除目前通用倍率和固定暴擊倍率造成的錯算（**暴擊機率與倍率是伺服器變數，安裝包無值**） |
| C02 | 部分 | 劍術大師完整傷害與費用 | 保留已確認的裸傷與里程碑；補齊英雄轉點擊、倍率組合與伺服器參數使用順序，避免重複計算 |
| C03 | 部分 | 英雄基礎傷害、升級費用與職業 | 37 位英雄逐級曲線、價格、地面／飛行／職業效果正確，替換近似 1.035 曲線 |
| C04 | 部分 | 英雄被動、武器、卷軸與昇階 | 逐一核對 370 被動及昇階次數；戰術洞察、武器套裝、卷軸套裝與增幅順序正確，未達門檻不生效 |
| C05 | 部分 | 泰坦生命、金幣、每關數量與頭目 | 使用正確縮放表及參數，補齊頭目分類與戰鬥時限；移除 1800 關硬上限（**需先完成 R04 的數值表示法，否則只能多推約 180 關**）；超時農場與重試一致 |
| C06 | 待做 | 濺射、跳泰坦、跳關及溢出傷害 | 各傷害來源的跳過能力分開計算；跨頭目、剩餘血量與首達關卡獎勵不漏領、不重複 |
| C07 | 部分 | 魔力系統與特殊回復 | 初始上限、基礎回復、點擊回魔、魔力虹吸、魔力馬尼、退款與施法耗魔依來源運作 |
| C08 | 部分 | 時間、攻擊節奏與離線演算 | 幀率、背景分頁、重新整理及批次計算結果一致；依原版上限實作離線金幣、潛行、推關與計時到期 |

C01 證據狀態：原生把暴擊參數放在 `[ServerVar]` 欄位——`playerCritChance`、`playerCritMult`、`maxCritChance` 與 `helperCanCrit`。
**安裝包的兩張伺服器變數表都沒有這四個鍵**，因此線上實際的暴擊機率與倍率不在包內，無法從安裝包核實。
唯一有原生預設的是 `helperCanCrit`：靜態初始化器在位移 0x6ec 寫入 0（RVA 0x24aafc0），即預設英雄不會暴擊；
該指令已加入 `tools/audit-servervars-parser.py` 的核對清單（目前 29 處指令），可對釘住的程式庫重跑。

現行引擎使用硬編的 2% 基礎暴擊機率與固定 10 倍暴擊傷害，**兩者都沒有安裝包依據**，已用測試釘住以便日後變更可見。
交接文件曾提到「原生預設 1%／11.5」，但本儲存庫沒有任何記錄或來源可追溯，因此不採用、也不寫入程式；
在取得線上伺服器變數值之前，C01 維持待證據，不以猜測值改動戰鬥數值。
## 第三階段：法術與全部戰鬥流派

| ID | 狀態 | 工作項目 | 驗收條件 |
|---|---|---|---|
| B01 | 部分 | 六個基礎法術 | 每級費用、效果、持續、冷卻、施法時點、到期與動畫；影分身不再只用連續傷害近似 |
| B02 | 待做 | 多重施法與新增法術 | 解鎖、層數、每層耗魔、持續、追加效果及套裝增益；不得只增加倍率按鈕 |
| B03 | 部分 | 手動點擊／火劍流 | 主動點擊、狂怒、額外點擊與觸發技能形成完整事件鏈 |
| B04 | 部分 | 寵物流 | 自動攻擊門檻、蓄力、雙重召喚、閃電爆發、閃光突進、特殊連擊及跳關均可實際觸發 |
| B05 | 待做 | 公會飛船流 | 加入公會條件、飛船攻擊、英雄協同、鼓舞、暈眩、蓄能、雷霆船及跳關 |
| B06 | 部分 | 影分身流 | 攻擊頻率、特殊嘗試、暴擊／致命、各法術關聯、特殊增幅與跳關 |
| B07 | 部分 | 天堂聖擊／暮光流 | 天堂傷害、暮光妖精、連續觸發、消耗及跨關處理 |
| B08 | 待做 | 匕首流 | 匕首生成、數量、冷卻、瞄準、弱點、毒、連擊、刀片流及跳關 |
| B09 | 待做 | 金槍流 | 普攻、黃金盛宴充能、金幣消耗、持續、攻速、黃金飛彈及跳關 |
| B10 | 部分 | 技能樹逐項收尾 | 以 R02 的 72 項資料列逐 ID 核對可用性；完成契約、雷擊等跨流派事件、前置、重配、技能點收入與重置規則 |

先完成 B01、B02，再按 B03 至 B09 各自完成整條事件鏈；B10 不以「有 72 個按鈕」驗收。尚未確認為啟用的資料列不能直接當成可購買技能。

## 第四階段：金幣、蛻變與神器

| ID | 狀態 | 工作項目 | 驗收條件 |
|---|---|---|---|
| E01 | 部分 | 所有金源 | 普通、頭目、寶箱、多重泰坦、妖精、米達斯之心與十倍金幣的實際事件、機率及收益正確 |
| E02 | 部分 | 特殊泰坦與妖精獎勵 | 波特、炸彈、魔力與其他已啟用特殊泰坦；妖精類型、出現間隔、技能／金幣／費用減免等效果與結束條件 |
| E03 | 部分 | 蛻變完整流程 | 聖物公式、當前／最高關卡區別、起始關卡、保留／重置欄位、技能點與里程碑領取 |
| E04 | 部分 | 神器發現與升級 | 103 件逐 ID 核對 8.2 效果；發現池、費用、批次升級、極高等級捨入與上限 |
| E05 | 待做 | 神器附魔、拆解與重抽 | 條件、順序、費用、退回資源與增幅正確；存檔可完整復原 |
| E06 | 部分 | 一般被動技能 | 依 PassiveSkillInfo 逐項完成解鎖、資源需求、費用與效果；確認天賦、神器與被動間的依賴 |

## 第五階段：裝備、寵物與增益

| ID | 狀態 | 工作項目 | 驗收條件 |
|---|---|---|---|
| I01 | 部分 | 裝備掉落、等級、品質、主副屬性 | 取代目前 best/10、只掉一般品質的近似分布；抽取條件與副屬性種類正確 |
| I02 | 部分 | 裝備操作與製作 | 穿戴、鎖定、販售、背包限制、碎片、工藝力量、製作費用及成長效果；不遺失收藏 |
| I03 | 部分 | 套裝全部效果 | 209 組資料逐 ID 核對啟用狀態與取得條件，完成特殊觸發、每日收益、自動技能等，不只永久倍率 |
| I04 | 部分 | 寵物成長與寵物蛋 | 每種寵物的等級段、出戰／被動、蛋等待與容量、多級獎勵分布及商店 |
| I05 | 待做 | 寵物任務與相關活動 | 任務條件、派遣、計時、獎勵、增幅、刷新與活動限定內容 |
| I06 | 部分 | 全部增益道具 | 補完黃金雨立即收益／離線升級、末日、滑動攻擊、腎上腺素及其他已啟用道具；區分模式專用道具 |

## 第六階段：日常與長期養成

| ID | 狀態 | 工作項目 | 驗收條件 |
|---|---|---|---|
| L01 | 部分 | 登入、每日任務、成就與教學 | 取代自訂兌換分布；使用確認的任務、獎勵、重置時區、補領與新手解鎖流程（**14 天登入獎勵已逐日對上客戶端實際讀取的欄位**；每日任務與教學仍待核對）。**成就已改用安裝包的 22 項並可領取鑽石（2.9.0）、每日成就改用安裝包的 10 項（2.10.0）、新手教學改用安裝包的 51 步（2.11.0）**，剩五項依賴未實作系統的每日成就 |
| L02 | 待做（部分待證據） | 商店與各種貨幣 | 商品、輪替、購買次數、鑽石、碎片、粉塵、兌換與廣告獎勵；實際付款／廣告供應流程另標明網頁差異。**寶箱商品本身有資料（18 種、鑽石價、解鎖關卡、各類件數），但開箱結果是伺服器回應，安裝包只有回應樣本，抽取規則無從還原** |
| L03 | 待做（部分待證據） | 泰坦靈魂、召喚與研究 | 依 TitanSummonBanner／TitanResearch／TitanCard 等表釐清系統，完成收入、抽取、研究費用與真實加成。**卡片效果、抽取機率、研究樹與升級費用皆有完整資料；靈魂與研究點的收入來源在安裝包內沒有值** |
| L04 | 待做 | 寶石與新增養成 | Gemstone 相關稀有度、召喚、等級、研究和效果逐表核對；確認解鎖與版本可用性 |
| L05 | 待做 | 超越／終局賽季、紀念碑、賽季寵物 | EndgameSeasonArtifact、EndgamePet 等表；賽季解鎖、重置、貨幣、紀念碑發現與升級、排名獎勵 |

## 第七階段：多人、公會與突襲

| ID | 狀態 | 工作項目 | 驗收條件 |
|---|---|---|---|
| M01 | 部分 | 多玩家身分、恢復與獨立存檔 | 10 位玩家／多裝置資料隔離；恢復憑證、備份、刪除與錯誤處理；不把暱稱當成唯一身分 |
| M02 | 待做 | 共用服務的交易、時間與事件驗證 | GAS／Sheets 採交易版本、伺服器時間、請求去重、配額與重試；驗證共享獎勵，防止雙領與舊狀態覆蓋 |
| M03 | 待做 | 公會 | 建立、加入、退出、角色權限、成員、聊天室／訊息、公告及公會進度；飛船解鎖與公會狀態連動 |
| M04 | 待做 | 突襲卡牌與玩家等級 | 所有已啟用卡牌逐 ID 完成效果、升級、粉塵、卡組與組合限制 |
| M05 | 待做 | 單人突襲 | 身體部位、護甲、生命、目標選擇、點擊限制、卡牌觸發、時限、世界／傳送門與獎勵 |
| M06 | 待做 | 公會突襲與大師階級 | 同步部位血量、攻擊次數、票券、忠誠、增益／減益、附魔、結算和獎勵；並發提交不重複扣血或發獎 |
| M07 | 待做 | 排行榜與一般錦標賽 | 本專案玩家的報名、分組、開始／結束、分數、同分處理與獎勵；官方全球配對資料未知時不宣稱相同 |
| M08 | 待做 | 深淵／特殊錦標賽 | 獨立暫存進度、起始資源、專屬神器池、模式加成、限制與結算；不能污染主存檔 |

多人共享功能需要 M02 先完成；M04／M05 可先在本機驗證，但不能因此把 M06 勾選完成。

## 第八階段：活動、外觀與操作體驗

| ID | 狀態 | 工作項目 | 驗收條件 |
|---|---|---|---|
| V01 | 待做 | 活動框架與時程 | 活動貨幣、任務、全球／公會／個人貢獻、結算與過期；歷史資料不一律當成常駐 |
| V02 | 待證據 | 小遊戲 | 釣魚、挖掘、狩獵、迷宮、落球、炸彈等內附模式逐一確認可用版本並完成；每種模式有獨立子清單。**六個模式的結果全部是伺服器回應**（見 [小遊戲界線](reference/tt2/8.2.0/minigame-server-evidence.json)）：抽到哪條魚／野獸、多大、幾點數、翻開哪一格、落球給多少獎勵，安裝包內都沒有值。可從包內核實的只有升級費用與加成、消耗品回復速度、收藏門檻這類客戶端算式 |
| V03 | 部分 | 戰鬥場景、角色與怪物呈現 | 地圖切換、怪物輪替、英雄位置、各流派攻擊特效、傷害數字、頭目與蛻變演出；逐場景對照參考。**已改用安裝包規則：14 主題、每 5 關切換、名稱官方繁中（2.7.0）；關卡怪物名單、頭目、載入隻數、繪製大小與背景循環分段（2.12.0）**。美術與顯示名稱仍是本專案自製——原版的小怪沒有名字，安裝包在地化檔也沒有。**傷害數字已於 2.12.3 分出三種來源**（點擊／暴擊、寵物攻擊、天堂聖擊），**2.12.7 依安裝包的 41 個 DamageType 核實分類並補上影分身**（見 [傷害回饋界線](reference/tt2/8.2.0/damage-text-evidence.json)）：影分身不再混在英雄每秒傷害裡，改為每 1÷攻擊速率 秒自己攻擊一次並冒出自己的數字。同一份證據也確認**受擊動畫不依來源上色**（包內十個受擊染色常數沒有任何方法讀取），所以不做依來源變色。**英雄位置已查**：原生確實有英雄站在戰鬥畫面上（`Helper` 元件帶一個 `PlatformPosition` 欄位，列舉只有 BottomLeft／TopLeft／BottomRight／MiddleBottomRight／MiddleTopRight／TopRight 六格），但**哪一位英雄站哪一格不在資料表也不在程式裡**——`HelperInfo` 只有 `IsFlying` 這類屬性，位置是場景 prefab 上編輯器指定的，安裝包的可讀資料裡沒有。要做只能自己排，不是還原。**仍待處理**：公會飛船／匕首／金槍三個流派的攻擊特效（戰鬥本身也未實作）、蛻變演出 |
| V04 | 部分 | 頁籤、面板與操作流程 | 英雄、法術、神器、裝備、技能、商店、公會、活動等畫面及互動完整；手機直向與桌面都可操作。**手機直向點按尺寸已於 2.7.2 處理**。**裝備的五部位總覽已於 2.13.8 補上**（點測 R-01：哪個部位裝了什麼、空的部位、背包裡有沒有更好的同部位裝備，以前都只能捲整份背包推敲），餘下的是尚未做出來的畫面本身 |
| V05 | 部分 | 繁體中文與原版術語 | 所有介面、動態訊息、效果與說明逐項核對；補齊目前缺漏／英文來源名稱及長文字，不直接照搬錯譯（**英雄、寵物、神器共 170 個名稱與官方繁中完全一致；套裝 158 個一致、3 個官方字串本身含未譯英文或為模板、20 個官方未命名**）。**103 個效果標籤已逐項對照官方字串（見 reference/tt2/8.2.0/bonus-label-parity.json），因安裝包用詞自相矛盾而維持既有用語**。**6 個主動技能與 66 個天賦已逐項對照（2.12.1）**：技能 6 個全部照官方、天賦 62 個相同、4 個官方字串本身夾英文而不照抄。**技能說明也已改用官方原文（2.12.6）**，技能這塊不再有自編文字 |
| V06 | 部分 | 音效、音樂、設定及外觀收藏 | 點擊、法術、掉落、升級、背景音樂、靜音與音量；頭像、稱號、外框等收藏；每項資源有來源與發布版本紀錄。**音效那半邊已於 2.11.4 完成**（9 種聲音、三段音量、逐項記在 ASSETS.md）。**設定另於 2.12.7 補上原版的傷害數字開關**：四個已實作來源（劍術大師、寵物、天堂聖擊、影分身）各自一個開關，分法與原生 CanShowDamageText 相同；原版另有的四個開關對應未實作的流派，不做成擺著不動的開關。**外觀收藏待條件**：583 個頭像中只有 74 個的解鎖條件是我們算得出來的（Stage 27、Milestones 19、PetLevel 15、EquipmentSet 10、Auto 3），其餘要賽事、活動、突襲或小遊戲；而且頭像與外框需要美術資源，安裝包素材不進版控。263 個稱號的解鎖類型全部是賽事／賽季／活動／Custom，在本專案沒有任何可達成的途徑，做出來會是一份全部鎖住的清單 |
| V07 | 部分 | 效能與載入 | 圖片預載／解碼、WebP、動畫排程、背景節流及大型列表；怪物切換無空白，十人使用下不因同步阻塞戰鬥。**已處理：戰鬥計算熱點（2.11.1）、圖片載入（2.11.2，首載圖片 4.9MB→1.2MB）、加成快取鑰匙與背景節流（2.11.3，dps 0.58ms→0.22ms）**。**大型列表已於 2.12.2 處理**（面板改為可被 React.memo 跳過，JS 時間每 5 秒 156ms→95ms）。剩下的是圖片解碼與動畫排程的細節 |

## 第九階段：完整性驗收

| ID | 狀態 | 工作項目 | 驗收條件 |
|---|---|---|---|
| Q01 | 待做 | 新帳號至終局流程 | 教學、解鎖、首次蛻變、各流派、終局／活動完整走通，沒有只能看的空功能 |
| Q02 | 部分 | 原版數值與時序比對 | 每個 ID 與事件鏈有測試結果；固定配置對照傷害、金幣、消耗、跳關和計時；公開列出仍未知差異 |
| Q03 | 部分 | 保存、遷移與故障演練 | 斷網、逾時、重複請求、換裝置、多分頁、瀏覽器背景、跨日及賽季切換不倒退或雙領 |
| Q04 | 部分 | 多人並發與共享狀態 | 10 位獨立玩家＋多裝置操作公會、排行榜、突襲與結算；權限、配額及恢復流程通過 |
| Q05 | 部分 | 發布與回復 | 每次更新有版本、紀錄、相容範圍及部署驗證；可回復前端並相容已寫入的新存檔；不以破壞玩家進度回滾 |

## 每次工作的固定流程

依「目前位置」取下一項 → 拆成可獨立驗收的小步驟 → 查資料與方法 → 實作事件／公式 → 補介面與存檔 → 驗證 → 更新本表與版本紀錄 → 發布並確認 → 將游標移到下一項。

如果一列太大，先在該列下建立子項目與完成證據，不把整列一次勾完。遇到來源缺口，記錄「缺哪個方法、欄位或操作紀錄」，先完成不依賴該缺口的部分；不以自訂值冒充原版。

## 完成紀錄

| 日期 | 項目 | 證據／結果 |
|---|---|---|
| 2026-09-17 | V04／V06 把技能施放、丟棄裝備、寵物派遣與蛻變後的畫面逐一點過，修掉兩處（2.13.9） | 這四項是前三輪點測累計沒測到的環節，這次用注入存檔（第 400 關、劍術大師 400 級、六技能各 5 級、四隻傷害寵物＋一隻支援寵物、五部位裝備）在線上逐一按過。**通過的**：施放火焰之劍後技能鍵轉為 `skill-active` 並倒數 46 秒、技能分頁同步顯示「45 秒」停用，天堂聖擊扣 14 魔力並冒出「☄️ 4.6穰」的金色數字；傷害位與支援位各自獨立換將，換完戰鬥區的寵物卡立刻跟著換；蛻變後關卡 400→001、金幣歸零、聖物 500→766（＋266 與畫面預告一致）、鑽石與最高關卡保留、轉生次數 0→1、自動跳到神器頁並跳出提示，裝備與寵物照原版設定保留不重置。**修掉兩處**：（一）丟棄裝備是不可逆的，原本一按就永久消失，沒有確認也沒有任何回饋——改成先展開一段確認（沿用 `globals.css` 裡本來就有、但從沒被用到的 `.inline-confirm`），寫明「永久消失，換不回來；已集滿的套裝收藏會留著」，並把手機上的兩顆鍵由量到的 36 像素高拉到 44，與 2.7.2 定下的點按下限一致；（二）寵物按鈕對「已經超過解鎖關卡、只是還沒開出來」的寵物寫「第 1 關加入蛋池」，讀起來像還沒開放，實際上牠早在池裡（`eggPool` 的條件是 `p.unlock<=best`，巴伯斯的 `unlock` 是 1 而玩家在第 400 關）——改成依 `s.best>=p.unlock` 分成「已在蛋池 · 等待孵出」與「第 N 關加入蛋池」。**找到但沒動的**：橫向手機 844×390 沒有任何高度媒體查詢，要捲兩頁多才看得到技能列；壓縮固定高度後留給怪物只剩約 41 像素，屬於版面重畫而非補一條覆寫，已寫進「待決定的取捨」第 10 條。421 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | V04 裝備的五部位總覽與同部位比較（2.13.8） | 補上點測三輪唯一沒修的那項（R-01）。裝備分頁最上面加了五個部位卡片：品質、等級、空槽用虛線框標「未裝備」；卡片本身是篩選鈕，點一下只列該部位。**「更好」的判準刻意窄**：同一個部位的效果不只一種（`TT2_GEAR` 靈氣 189 件裡 `HelmetBoost` 57、`SwordBoost` 67、`SlashBoost` 62，劍也有 `AllDamage`／`TapDamage`／`CritDamage`／`AllHelperDamage` 四類），跨效果的兩件比不出高下，所以 ▲ 只認同部位、同效果、`equipmentValue` 更高的三個條件都成立；部位空著時任何一件都算贏。這樣不會對玩家說謊，代價是換流派用的替代品要自己挑。**順手修掉一個真的顯示缺陷**：頭盔圖示 `🪖`（U+1FA96，Emoji 13）在這台 Windows 10 上沒有字型，畫布量測 ink 159／colored 0——是 tofu 方框不是圖，換成 `⛑️`（ink 1307／colored 581）；這個圖示原本在裝備列表就用了，不是這次引入的。**版面**：升級面板只有約 330 像素寬，一格分到 57–60 像素，而 780px 以下有一條把 `.equipment-slots small` 放大到 12px 的舊規則會讓「等級 42」換行，依 AGENTS.md 的規則順序約定在 `globals.css` 末尾壓回 11px。以注入存檔（劍等級 10 已裝＋等級 40 在背包、頭盔 25 已裝、衣服空＋背包一件、靈氣 12 已裝＋背包 8、砍痕全空）在 1280／834／390 三種寬度實測：五格文字與 class 全部符合預期、無格內溢出、無橫向捲動、主控台無錯誤；實際按下「裝備」後總覽即時由「等級 10 ▲1」變成「等級 40」且 ▲ 消失；點空的砍痕槽顯示「背包裡沒有砍痕」，再點一次回到全部六件；空背包時五格全部虛線「未裝備」且沒有任何 ▲。421 項 Node 測試＋23 項 Python 測試通過，`tsc --noEmit` 無誤。 |
| 2026-09-17 | V03／V04／V06 用真實瀏覽器點測三輪，修掉 14 處介面問題（2.13.6、2.13.7） | 把八個分頁在桌面 1280、平板 834、手機直向 390 三種寬度點過一遍，**每一步截圖判讀**，並用「乾淨存檔」與「第 1200 關、8 英雄、五槽位各一件裝備、寵物、套裝」兩種存檔各跑一次。**最有價值的是一個自動化測試抓不到的真 bug**：世界地圖推過第 3080 關之後就凍結在「關卡 3066—3070」那一組，第 5000 關與第 98000 關看到的都一樣。根因是 `BACKGROUND_CYCLE` 最後一列的 `level` 是 3065，第 3066 關之後每一關都落在那一列，而 `bandStart` 直接讀該列的 `level` 當起點。一段涵蓋的關卡數就是 `themeEnd`（`tests/stage-monsters.test.mjs` 原本就在驗這件事），改成在列內再依 `themeEnd` 分輪，第 98000 關現在顯示 97996—98000，早期關卡不變；新增的回歸測試已用 mutation 驗過會轉紅。**第二個是文案指向錯的階數**：成就卡寫「本階獎勵 💎10」但按鈕只給 💎5——`diamondReward` 是 0-based，`[tier]` 是下一階的獎勵，按鈕用的 `slice(claimed, tier)` 一直是對的，改成「下一階獎勵」。**其餘 12 處**：雲端存檔未建立時仍顯示「停止自動同步」（按了還說「雲端存檔均保留」且無恢復路徑，補上與「立即同步雲端」相同的 `active` 守衛）；`h3` 裡的狀態標籤與名稱沒有間距（「闇影之書等級 0」「魔力藥水0 / 3 層」，一條 `h3 small{margin-left:6px}` 全解）；手機與平板的寵物卡壓住傷害數值；手機資源列的大數字被裁切；戰鬥區底部三段文字擠成三欄逐字換行；「點擊巨獸，揮劍出擊」與數值重疊；寵物與技能樹的空狀態沒有取得途徑（補成「到收藏開寵物蛋，每 4 小時補一顆」與「第 100 關起每 50 關 1 點」）；設定的總音量預設 70 但兩個子項是 0，看起來有聲音其實全靜音（補一句三段相乘的說明）。**踩到的坑記在這裡**：`app/globals.css` 的規則不照選擇器分組，同一個 class 的基礎宣告常在檔案很後面（`.pet-combat-status` 在 29800 附近），手機覆寫插在前面會被整條蓋掉——踩了三次才改成一律加在檔案末尾。另外**只測空存檔會漏掉一整類問題**：資源列與寵物卡的第一版修法在有資料時反而更糟（從「換行看得到」變成「截斷看不到」），資源列前後改了三次才對。**唯一沒修的**是裝備缺少「五個部位各裝了什麼」的總覽，那屬於 V04 尚未做出來的畫面，是新功能不是修 bug。421 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | I06 黃金雨立即金幣的形狀讀出來了，連同接不上的具體原因 | 登記表原本把它和隨機配發、票券寫在同一條，只說「尚未還原」。這次拆開並讀出形狀：`GetMakeItRainGold` 先挑一個關卡——把「歷史最高 − 目前」的差額乘 `makeItRainMaxStageMult`（0.85）再加回目前關卡，和 `makeItRainStageMult`（1.0）× 目前關卡兩者取其一——再交給 `GetPerkGold`；`GetPerkGold` 則是 `(GetLargestGoldSourceAmount(關卡) × Bonus(PerkGold)) ^ perkGoldReduction`，指數 0.9965 略小於 1，作用是把很大的金幣值壓下來。**所以金額根本不是關卡的函數**，而是「玩家在那個關卡最大的單一金幣來源值多少」。**接不上的原因因此很具體**：`GetLargestGoldSourceAmount` 有 387 條指令，對寶箱泰坦、頭目、妖精、寵物點金手四個來源取 `GHDouble.Max`，還要 `GetMaxHandOfMidasBonus` 與一次 QTE 解鎖檢查——這四條本專案沒有一條是照原生算的，先還原它們才談得上立即金幣。新增 `tools/audit-perk-gold.py`（兩個方法的位元組雜湊、三個 `[ServerVar]` 偏移、`Bonus(PerkGold)` 的列舉值、`perkGoldReduction` 確實是最後那個 `Pow` 的指數、以及 `GetLargestGoldSourceAmount` 的四個來源都還在）、`reference/tt2/8.2.0/perk-gold-evidence.json` 與 `tests/perk-gold.test.mjs` 5 項。沒讀的部分列名：`GetMakeItRainGold` 的兩層 `fcsel` 條件碼、`GetLargestGoldSourceAmount` 本體、以及相鄰的 `perkGoldSkillPointBase`（1.04）與 `perkGoldSkillPointMaxCount`（400）的用途。登記表 70→71 項來源，table-differs 11→12（拆項所致）。未改任何遊戲數值，版本號不變。420 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | E01 累加倍率那一項展開，聖物外層四個乘數全部對上（2.13.5） | 上一條把外層的結合順序讀完，但把累加倍率那一項記成「需要整套系統、沒有安全預設值所以不接」。**展開之後發現不必接**：`GetCurrentAdditiveRelicMultiplierBonus` 只有十幾條指令——`1 + stageRushToRelicMultiplier（float 0.00017）× PrestigeModel.AdditiveRelicMultiplier`（已擁有的數量，欄位偏移 0x48）；`GetRewardableAdditiveRelicMultiplierAmount` 則是 `Math.Max(0, GetNextAdditiveRelicMultiplier() − 已擁有)`。整項因此化簡成 `1 + 0.00017 × Max(已擁有, 下一個門檻)`，**在沒有累加倍率的存檔上恆為 1**——也就是說引擎原本省略它是對的，不是漏掉，狀態由 `table-differs` 改為 `native`。順帶把**進位方向**也改了：原生是 `GHDouble.Ceiling`，引擎原本 `Math.floor`，已改成 `Math.ceil`，每次蛻變固定多一顆（第 60 關 10→11、第 1000 關 1258→1259）。改用進位之後專案自訂的 `max(1, ·)` 幾乎不再生效（任何正數進位後至少是 1），保留當作守門。**至此聖物外層四個乘數與進位全部與原生對上，只剩曲線本體仍是 7.5 近似**（待決定第 9 條）。順帶記下沒往下追的部分：累加倍率的取得條件那一組方法（`GetRequiredStageForAdditiveRelicMultiplier` 走一個 `PerformStageScalingFormula`、`GetAdditiveRelicMultilpierCap` 是 `Min(additiveRelicMultiplierMax 50000, additiveRelicMultPrestigeMaxPercent × 季節最高關卡)`）整組未查，而且它依賴 `PlayerModel.GetSeasonalMaxStageReached`，季節系統本專案也沒有。`tools/audit-relic-curve.py` 增加兩個方法的位元組雜湊與形狀斷言（已擁有欄位偏移、0.00017 的載入、`fmov #1.0`、`Max(0, next − owned)` 的 `sub`＋`mov w0, wzr`）。登記表 70 項不變，native 11→13、table-differs 13→11。415 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | E01 聖物外層乘數的順序讀完，補上兩個原本沒算進去的加成（2.13.4） | 上一條把曲線本體讀完，這條把外層讀完。**難點是 GHDouble 運算子都走 out 指標**，所以要跟著每個結果落在哪個堆疊槽、下一個呼叫又讀回哪一槽，才看得出結合關係；把 `add xN, sp, #imm` 建立的槽位追一遍之後順序就定了：`Ceiling(曲線值 × Bonus(PrestigeRelic) × (1 + Bonus(PrestigeRelicAdditive)) × 累加倍率 × Bonus(OnlyPrestigeRelic))`，其中那個 1 是 `mov w0,#1` 的立即數，累加倍率那一項是 `GetCurrentAdditiveRelicMultiplierBonus() + stageRushToRelicMultiplier × GetRewardableAdditiveRelicMultiplierAmount()`（係數預設值 float 0.00017）。**接上其中兩個**：`(1 + PrestigeRelicAdditive)` 與 `× OnlyPrestigeRelic`。兩者在乾淨存檔都是無作用值（加法型預設 0、乘法型預設 1），所以第 1000 關的聖物仍是 1258 不變；**湊齊神話套裝才會變多**——包內 17 個神話套裝各給 `PrestigeRelicAdditive` 2.4408，湊一套就是 3.44 倍（第 1000 關 1258→4331），資料本來就在 `TT2_SETS` 裡只是沒被讀。`OnlyPrestigeRelic` 在引擎資料裡目前沒有任何來源，接上是為了位置正確。**累加倍率那一項沒接**：它背後整套系統本專案沒有實作，也沒有安全的預設值（乘 0 會讓聖物歸零），照實列為 `table-differs`。`tools/audit-relic-curve.py` 增加外層的呼叫順序斷言（四次 `op_Multiply`、三次 `GetBonus`、結尾 `Ceiling`，整串順序逐項比對）與 `stageRushToRelicMultiplier` 的偏移檢查；`tests/relic-curve.test.mjs` 7→8 項，新增「乾淨存檔不變、湊齊套裝剛好 1+2.4408 倍」。登記表 69→70 項來源，native 10→11。415 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | E01 聖物曲線第三段讀完，整條算式還原（未採用） | 先前登記寫「算式只讀出三段中的兩段，第三段含 `Math.Min` 與多層 `Math.Pow` 尚未完整還原」。這次讀完了：第三段的**指數本身**是 `Min(relicStageExpoMax3, relicStageExpo2 × (1 + relicStageMult3 × 關卡^relicStageExpo3))`，拿它當關卡的指數之後，再以 `relicStageBase2` 為底做 `GHDouble.Pow`——會用 GHDouble 而不是 `Math.Pow`，正是因為這一段的結果會超出 double。完整算式：關卡先 `Min(關卡, relicsStageMax)`，三段為 `1.5 × (關卡 − 56)`、`3 × 1.21^(關卡^0.48)`、`1.002^(關卡^min(1.0155, 1.005×(1 + 5e-07×關卡^1.1)))`，相加後 `Max(0, ·)`。**十個係數全部有編譯期預設值**（先前記為八個，`relicStageExpo3` 1.1 與 `relicStageExpoMax3` 1.0155 也有）。**沒有採用**：第 2000 關以內原生是引擎的 1.6–3.4 倍（同數量級），第 10000 關起第三段的指數頂到 1.0155、`1.002^(關卡^1.0155)` 開始主導，第 30000 關差 10^24、關卡上限差 10^95——整條換掉等於重做蛻變經濟，而且與怪物曲線那條配套（原生聖物給得多，是因為原生血量在第 250 關後遠低於引擎），寫進「待決定的取捨」第 9 條並附逐關對照。**順帶查出外層還有兩處同源的不一致**：`GetTotalRelicsFromStageCount` 依序乘上 `PrestigeRelic`、`PrestigeRelicAdditive` 與 `OnlyPrestigeRelic` 三個加成，引擎只套用第一個（後兩者的資料在 `TT2_SETS` 裡卻沒有任何地方讀它）；原生以 `GHDouble.Ceiling` 進位，引擎用 `Math.floor` 捨去。**另外更正兩個先前寫錯的數字**：登記表說「31 個含 relic 的 `[ServerVar]` 只有兩個有值」，那是變數表的狀況——按編譯期預設值算 **31 個裡有 28 個有值**；`additiveRelicMultiplierMax` 是 **50000** 不是 150000。也就是說季節聖物與累加倍率那幾條乘數其實查得到，只是還沒去還原算式。新增 `tools/audit-relic-curve.py`（兩個方法的位元組雜湊、十一個 `[ServerVar]` 偏移、四次 `Pow` 與那個 `Min` 的先後、兩次加法與結尾 `Max`、外層三個 `GetBonus` 的型別都會斷言；`ldp` 一次載入兩個相鄰係數，掃描要一併認）、`reference/tt2/8.2.0/relic-curve-evidence.json` 與 `tests/relic-curve.test.mjs` 7 項（測試自己用係數重算一次曲線，不是照抄證據裡的數字）。登記表 67→69 項來源，table-differs 10→13、server 4→3。未改任何遊戲數值，版本號不變。414 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | E01 蛻變門檻第 60 關查到原生欄位，登記表的 `baseline-75` 歸零 | 登記表最後一項 `baseline-75`。先前寫「安裝包未見對應的蛻變開放關卡欄位」，**那是沒找到名字**：`[ServerVar]` **`minimumPrestigeStage` 的編譯期預設值就是 60**。找法換了一種——按名稱搜尋找不到，就改**按指令編碼掃描**：`ldr Wt,[Xn,#0xbec]` 是 `1011_1001_01 imm12 Rn Rt`，除了兩個暫存器欄位以外完全固定，對整個映像做一次遮罩比對就找出全部三個讀取點（`PrestigeModel.GetPrestigeStage`、`AchievementPanelScript.InitMilestonesPanel`、`TutorialEventModel.CheckAlreadyCompleted`）。關鍵的那個在 `GetPrestigeStage` 的**尾端**：整個方法結束在 `b System.Math.Max`，而 `ldr w1,[x8,#0xbec]` 正是它的第二個引數，所以 60 是「這次蛻變要打到第幾關」的**下限**；`CanPrestige` 只是拿關卡和它比 `>=`。引擎原本把 60 硬寫在 `relicGain`、`discover`、`prestige` 三處，現在抽成 `PRESTIGE_DEFAULTS.minimumStage` 共用，狀態由 `baseline-75` 升為 `default`。**順帶查出原生在下限之上還有一層**：`GetPrestigeStage = Math.Max(floor(`prestigeMsPercentRequirement` × (基準 − 進階起點) + 進階起點), `minimumPrestigeStage`)，係數預設值是 float **0.5**、基準取自 `maxPrestigeStageCount`，也就是**要推到歷史最高的一半才能再蛻變**；引擎沒有這一層，列為 `table-differs` 並寫進「待決定的取捨」第 8 條。新增 `tools/audit-prestige-unlock.py`（三個方法的位元組雜湊、三個 `[ServerVar]` 偏移、以及「那個 `Math.Max` 是尾呼叫而且在讀取 `minimumPrestigeStage` 之後」都會斷言）、`reference/tt2/8.2.0/prestige-unlock-evidence.json` 與 `tests/prestige-unlock.test.mjs` 6 項。沒讀完的部分逐項列名：`CanPrestige` 左運算元那個 ObscuredInt 的身分、`GetPrestigeStage` 前半由 bool 開關選的分支、`GetAdvancedStartNextStage`、以及另外兩個讀取點的用途。登記表 66→67 項來源，**`baseline-75` 1→0**、default 14→15、table-differs 9→10。抽常數不改行為，未改任何遊戲數值，版本號不變。407 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | I04 寵物成長逐式對上 8.2，改良段的三處不一致列入待決定 | 登記表裡 `baseline-75` 的倒數第二項。`PetInfo` 的六個方法都讀得完，**大部分是「查得到而且一樣」**：三段傷害 `damageBase + inc1×min(等級,L1) + inc2×min(max(等級−L1,0),L2−L1) + inc3×max(等級−L2,0)`，兩個門檻就是 `[ServerVar]` `petDamageIncLevel1`／`2`（40／80）；未出戰的比例是 `Min(1, gap × increment × (等級 ÷ gap))`（整數除法），`petPassiveLevelGap` = 5、`petPassiveLevelIncrement` = float 0.01，相乘後每 5 級 5%，與引擎相同；`GetPassiveDamage` 就是主動傷害乘該比例，`GetPassiveBonus` 則是 `identity + (主動值 − identity) × 比例`（identity 取自 `BonusModel.GetBonusIdentity`），與引擎的 `additive ? full×fraction : 1+(full−1)×fraction` 相同；`GetActiveBonus` 的 `(bonusBase + bonusInc×等級) × 改良乘數 × Bonus(...)` 三個因子位置也相同。**引擎的 30 隻寵物資料與 8.2 的 `PetInfo` 逐格核對全部相同**（含 `MaxImprovementLevels` 與三段 `DamageInc`）。**不一樣的是改良段，而且是三處**：原生 `Min(Floor((等級 − 100) ÷ 20), maxImprovementLevels)`，引擎 `max(0, floor((min(等級, maxImprovementLevels) − 100) ÷ 50))`——每段的等級間隔 **20 對 50**、`maxImprovementLevels`（表裡 1600）原生夾的是**段數**而引擎夾的是**等級**、原生**沒有**把段數夾到 0 以上（所以 100 級以下乘數小於 1）。**沒有採用**：以 `improvementBonus` = 1.5 的 21 隻為例，等級 1 時原生只有引擎的 13%、100 級相同、200 級 3.4 倍、1000 級 5.7×10⁴ 倍、1600 級 8.4×10⁷ 倍，早期變弱後期暴增，等於整條寵物曲線重來，寫進「待決定的取捨」第 7 條。順帶記下 `PetType.Endgame` 另走 `endGamePetImprovementLevelStart`／`Delta`（皆為 6），但 `PetInfo` 的 30 隻只有 Legacy 與 Exotic，用不到。新增 `tools/audit-pet-growth.py`（六個方法的位元組雜湊、`ParsedPetInfo` 八個欄位偏移、八個 `[ServerVar]` 偏移與 `PetType` 列舉值都先驗一次才往下讀；`GetImprovementBonus` 把偏移折進位址再載入，掃描要同時認 `add` 那一形）、`reference/tt2/8.2.0/pet-growth-evidence.json` 與 `tests/pet-growth.test.mjs` 6 項。`petBonus` 由「非公式」升為獨立的公式條目。登記表 62→66 項來源、30→31 條公式，**baseline-75 2→1（只剩聖物開放門檻第 60 關）**、native 8→10、table 17→18、default 13→14、table-differs 8→9。未改任何遊戲數值，版本號不變。401 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | I06 增益 perks 對上 8.2 原生，補回滿層與黃金雨兩處差異（2.13.3） | 登記表裡 `baseline-75` 最後剩三項，這次查掉一項。**數值那半是「查得到而且一樣」**：8.2 的 `PerkInfo.json` 就在參考目錄裡，ManaPotion 給 `AllManaGained` 1.5／1.75／2／2.25、MakeItRain 給 `AutoBuyHeroes` 45／15／5／3，兩者都是 43200 秒、基礎費用 100，與本專案沿用的 7.5 值逐格相同；鑽石價另有同值的編譯期預設值 `perkDiamondCost` = 100。層數上限也對得上：`PerkModel.get_CurrentMaxPerkStackAllowed`（RVA 0x2383d18）就是 `mov w8,#3` 加一個 `cinc`——上限是 3，`Bonus(PerkMaxLevel)` 大於 0 時 4，而 `MAX_PERK_STACK = 4` 同時是 `ActivePerkInfo.timers` 的固定格數（`ShiftTimersAndCountStack` 走的正好是 32 位元組），所以引擎的 `min(4, 3 + PerkMaxLevel)` 與原生等價；`GetBonusAmountA` 以 `stackCount−1` 當索引、兩端各夾一次，也與 `values[min(3, n−1)]` 相同。`RunPerkTimer` 對每一層各自扣時間，證實「每層獨立計時」無誤。**但有兩處引擎沒照做，這次補上**：（一）**滿層不是拒絕**——原生 `ActivatePerk` 在 `stackCount` 等於上限時先呼叫 `RemoveOldestStack`（把仍在跑的計時器排序後清掉剩最少的那格、`stackCount` 減一）再加新的一層，等於用一次使用機會把快結束的那層續滿；引擎原本直接 `return false`、按鈕也是灰的，現在照原生放行並改了文案。（二）**黃金雨的間隔另有乘數**——`GetBonusAmountA` 只對 `PerkID.MakeItRain`（值 1）多走一段 `1 − min(Bonus(AutoBuyHeroesMultDuringMakeItRain), autoBuyHeroesMaxBonus)`，上限欄位的編譯期預設值是 float 0.95；包內給這個加成的是 **GoldRain 傳說套裝（0.3）**，資料本來就在 `TT2_SETS` 裡，只是沒有被讀，所以湊齊該套裝後間隔剩七成（三層由每 5 秒變每 3.5 秒）。魔力藥水不吃這個乘數。**沒做的部分逐項列名不只給數量**：原生的 perk 隨機配發在第 1200 關解鎖（`perkSelectUnlockStage`）、另有票券制（`perkTicketCost` = 10），黃金雨的立即金幣走 `GetMakeItRainGold`，以 `makeItRainStageMult`（1.0）與 `makeItRainMaxStageMult`（0.85）配合目前與歷史最高關卡計算；`PerkInfo` 共 19 列，本專案只實作兩列。新增 `tools/audit-perks.py`（八個方法的位元組雜湊、PerkID 與 BonusType 的列舉值、`ActivePerkInfo` 四個欄位偏移都先驗一次才往下讀）與 `reference/tt2/8.2.0/perk-evidence.json`，以及 `tests/perks.test.mjs` 由 4 項增為 11 項（原本那 4 項裡「滿層第四次不扣鑽石」的預期依原生行為改寫）。登記表 58→62 項來源，baseline-75 3→2（只剩聖物開放門檻第 60 關與寵物每段增量）、native 6→8、table 16→17、default 12→13、table-differs 7→8。395 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | R04 修正超界等級讓加成變 NaN（2.13.2） | 2.13.1 上線後在線上複測，看到「魔力 1e+240 / 1e+240」。追下去發現不是新回歸（舊版在同一份存檔會顯示 NaN，只是被 `limit()` 換成引擎天花板而已），而是一個一直都在的缺陷：**天賦的數值陣列只到該天賦自己的上限**（3／9／15／20／30／40／45 不等），`baseFrom` 卻直接用存檔裡的等級當索引——`MPCapacityBoost` 上限 15，存檔若寫 45 就讀到 `undefined`，`total + undefined` 變成 NaN，接著 ManaPoolCap、魔力上限、每秒傷害全部被污染。主動技能同理：`amount`／`mana`／`cost`／`second` 四張表都以 `等級−1` 當索引，超過 30 級就讀到 undefined。**兩層都修**：讀表時夾到最後一格合法值（新增 `skillStep`），`hydrate` 另外把存檔裡超界、負數或非數字的天賦與技能等級夾回合法範圍，讓壞掉的存檔載入一次就修好。線上複測時還看到技能鍵寫「1e+240 秒」——那是同一類的另一半：神器等級不索引表格，但購買路徑到不了的等級一樣會把持續時間加成推到引擎天花板，所以 `hydrate` 也把神器等級夾回 `apply()` 買的時候檢查的那個上限。新增 `tests/out-of-range-levels.test.mjs` 5 項，直接以「全部天賦 45 級、全部技能 99 級、全部神器 100 萬級」的存檔驗證加成、傷害、魔力上限與技能持續時間都回到有限值。另外存檔裡殘留的技能計時器本身也可能是天文數字（在加成還壞著的時候寫入的），會讓技能永遠顯示施放中，因此 `hydrate` 把超過載入時間起算 24 小時的 `active` 與 `cooldowns`（以及非數字）一律視為已結束——夾到 24 小時等於白送一天的技能。另外魔力上限為 0（還沒解鎖任何主動技能）時，介面不再寫「魔力 0 / 0」，改為說明第一個技能在劍術大師幾級解鎖。388 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | C07 魔力上限改用原版算法，回復算式核對相同（2.13.1） | `PlayerModel.RefreshManaRegen` 的最後兩步讀完了：`(manaRegenBaseInMinutes + Bonus(ManaRegen)) × Bonus(ManaRegenMult) ÷ 60 × Bonus(AllManaGained)`——**除以 60 那一步證明 2 的單位是「每分鐘回復點數」**，與引擎既有算式完全相同，只補上原本漏掉的 `AllManaGained` 乘數，狀態由 `table-differs` 改為 `default`。上限那半則確實不同：`RefreshManaCap` 走 `ActiveSkillModel.GetSkillManaCapAmount`，從 `manaCapInitial`（0）起算、每個已解鎖的主動技能加 `manaCapPerSkill`（35），再套 `ManaPoolCap` 與 `ManaPoolCapPercent`。引擎改用這個算法：劍術大師 100 級解鎖第一個技能（天堂聖擊，8 點魔力）前上限為 0，350 級之後六個全解鎖是 210——對已經全解鎖的存檔等於 200→210，幾乎無感，但早期不再有用不完的魔力。八個測試因此改寫，改用「每個已解鎖技能 35」表達期望值而不是寫死 200。381 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | C07／B04 寵物兩項來源升級，魔力兩項改判 | 繼續用靜態預設值總表回頭校正。**寵物**：`petTapAmount` 的編譯期預設值就是 **20**，與引擎「每 20 次有效點擊發動一次」相同；`petDamageIncLevel1`／`petDamageIncLevel2` 是 **40** 與 **80**，與引擎把寵物等級切成 0–40／40–80／80 以上三段的門檻也完全相同——兩項都不必改數值，只是來源由 `baseline-75` 升為 `default`（每段乘多少仍是 7.5 的 PetInfo，維持 baseline-75）。**魔力**：原生沒有固定上限，`PlayerModel.RefreshManaCap` 走 `ActiveSkillModel.GetSkillManaCapAmount`，從 `manaCapInitial`（0）起算、每個已解鎖的主動技能加 `manaCapPerSkill`（35），再套 `ManaPoolCap` 與 `ManaPoolCapPercent`；六個技能全解鎖是 210，早期則遠低於引擎固定的 200。回復那邊 `RefreshManaRegen` 以 `manaRegenBaseInMinutes`（2）為底、加 `Bonus(ManaRegen)`、乘 `Bonus(ManaRegenMult)`，最後還有一次除法與一次乘法換算成每秒量，**那兩步還沒讀完**，所以連 2 的單位是「每分鐘幾點」還是「幾分鐘回滿」都還不確定。兩項都改列 `table-differs` 並寫明未採用的理由——上限與回復是配套的，只改一半會失衡。381 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | E01／B01／C05 三項來源改判，聖物公式讀出一半 | 用靜態預設值總表回頭校正三筆登記。**聖物**：`PrestigeModel.GetBonusRelicsFromStageCount`（RVA 0x23ff6cc）讀的 `ldr d9,[x8,#0x170]` 這類偏移，逐一對上 `ServerVarsModel` 的靜態欄位——`relicStageMult1` 3、`relicStageMult2` 1.5、`relicStageMult3` 5e-07、`relicStageBase` 1.21、`relicStageBase2` 1.002、`relicStageExpo` 0.48、`relicStageExpo2` 1.005、`relicStageOffset` −56，**八個係數全部有值**，先前登記的「安裝包一個值都沒帶」只對變數表成立。算式**只讀出一部分**：關卡先被 `relicsStageMax` 夾住，之後至少三段（1.5 × (關卡−56)、3 × 1.21^(關卡^0.48)、一段含 `Math.Min` 與多層 `Math.Pow` 的 1.002 系項）以 GHDouble 組合，尚未完整還原，所以引擎維持 7.5 近似，狀態由 `server` 改列 `table-differs`。**技能點**：`skillPointsStageDelta` 500、`skillPointsStageMin` 51——引擎的「每 50 關一點」比原生寬鬆十倍，照改會讓第 2000 關的點數從 39 掉到個位數，列入「待決定的取捨」第 0.5 條，狀態改 `table-differs`。**頭目計時**：`bossPlayTimeBase` 的預設值就是 30，與引擎沿用的數字相同，狀態由 `server` 升為 `default`，不必改數值。381 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | C01 暴擊與寶箱改用原生基礎值，影分身節奏更正（2.13.0） | 找到一張先前沒人看過的表：`BonusModel.SetDefaultBonuses`（RVA 0x27147c4）在啟動時建立「每個加成的基礎值」字典，每一筆都是 `Dictionary.Add(BonusType, 某個 ServerVarsModel 靜態欄位)`。`tools/audit-bonus-defaults.py` 逐筆配對，**14 筆全部配對成功、0 筆落空**，並交叉核對每個值與靜態預設值總表相同。接進引擎的三項：**暴擊**——`CritChance` 的基礎值是 `playerCritChance` 0.01（引擎原本 0.02），倍率照 `PlayerModel.RefreshCriticalValues` 用 `playerCritMult` 11.5（原本 10），上限 `maxCritChance` 1；機率算式也照原生改成 `(基礎＋加成) × Bonus(AllProbabilityBoost)`。**順帶修掉一個實作缺陷**：點擊那一行寫死 `lastCrit?10:1`，根本沒有用自己匯出的 `critMultiplier`，所以暴擊傷害一直不吃 `CritDamage` 加成；現在會吃了。**寶箱泰坦**機率的基礎值是 `chestersonChance` 0.01（原本 0.02），同樣乘上機率加成。**影分身**：`ShadowCloneSkillAttackRate` 的基礎值是 `baseShadowCloneAniPerSec` = **4**，所以 2.12.7 做的「每秒一次」是錯的，更正為每秒四次；`buildDamage` 是本專案的每秒總量近似，因此一次攻擊取其 ÷ 速率，節奏照原生而總量不變。其餘 11 個基礎值（多重泰坦 0.01、上限 4 隻、炸彈泰坦 0.001、波特 0.001、十倍金幣 0.01、魔力馬尼 4、雙寵爆發 5 次…）先記錄在證據檔並以測試釘住，還沒接。`critical` 與寶箱機率由 `server` 改列 `default`。新增 `tests/bonus-defaults.test.mjs` 6 項；`tests/crit-evidence.test.mjs` 裡那個「引擎的暴擊數字沒有包內依據」的測試改寫為新結論。381 項 Node 測試＋23 項 Python 測試通過。線上實測時發現每秒四下的數字會疊成一坨（同時最多八個），已改為每次落點略為散開、存活時間由 850ms 縮到 420ms。 |
| 2026-09-17 | R05 缺口盤點補上第二欄，C／I 兩段的「卡在證據」結論作廢 | `tools/server-var-gaps.mjs` 原本只回答「變數表有沒有帶這個鍵」，現在同時回答「程式裡有沒有編譯期預設值」，並多算一欄「兩者皆無」。結果推翻了先前的判斷：953 個 `[ServerVar]` 中 **769 個有預設值**，兩者皆無的只有 **183 個**。分組後 C05 泰坦與金幣 67 個中 61 個有、**I01–I03 裝備 26 個全部都有**、C01 暴擊 10 個中 8 個、C03–C04 英雄 22 個中 19 個、C07 魔力 18 個中 13 個、C02 劍術大師 4 個全部都有。`docs/server-var-gaps.md` 改寫為兩條線索並列，`server-var-gaps.json` 的每個欄位多帶 `compiledDefault` 與 `defaultValue`。ROADMAP 的「目前位置」同步修正——**C／I 兩段不再是「卡在證據」**，只是引用預設值時狀態為 `default`（線上可覆蓋）。順帶記下幾個之後會用到的值：`playerCritChance` 0.01、`playerCritMult` 11.5、`maxCritChance` 1、`bossPlayTimeBase` 30（與引擎現值相同）、`chestersonChance` 0.01、`maxPetEggs` 2（與引擎相同）、`skillPointsStageDelta` 500／`skillPointsStageMin` 51、`relicStageBase` 1.21／`relicStageExpo` 0.48／`relicStageMult1` 3／`relicStageOffset` −56、`ascendCostExpoScaling` 2532.5（這一個先前記為「無值、所以英雄昇階費用接不上」）。這些都還沒接進引擎，接的時候要逐項反組譯確認用途。374 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | C05 怪物血量與金幣曲線逐式還原，連同係數一起（未採用） | 先修好解析器的兩個盲點：一是 GHDouble 靜態欄位——它是以 `GHDouble..ctor(double)` 建在堆疊上再把 24 位元組複製進靜態區塊，要抓的是**建構時的那個 double**；二是 capstone 會把小的 `movk` 立即數印成十進位，原本的正則只吃十六進位，漏掉了 17 個欄位。修好後解出 **770 個**預設值（原 745）。接著逐式還原 `MonsterModel.GetMonsterBase`（RVA 0x2325c4c）：呼叫順序就是算式——`Math.Min` → `GHDouble.Pow` → `op_Multiply` → `Math.Max`／`Math.Pow` → `GHDouble.Pow` → `op_Multiply` → 同一組再一次 → `op_Division`，也就是 `mult × base1^min(關卡, levelOff) × base2^(expo1 × max(關卡−levelOff,0)^expo2) ÷ base3^(expo3 × max(關卡−transcendenceLevelOff,0)^expo4)`。血量與金幣共用它，十個係數的預設值全部解出（mult 17.5、base1 1.38、base2／3 為 10、levelOff 100、血量 expo 0.03／1.098、金幣 expo 0.04／1.036、transcendenceLevelOff 180000——高於關卡上限 98000，所以除數在可玩範圍恆為 1）。`tools/audit-monster-curve.py` 除了釘住呼叫順序，還輸出 12 個關卡的原生與引擎對照（以常用對數表示，數值本身會溢位）：第 100 關原生較高，**第 250 關起原生遠低於引擎**，第 2000 關差 10^107、第 98000 關差 10^2745，金幣同向。**沒有採用**：這等於整條難度曲線換掉，而且必須與英雄傷害那條（多出來的 1.035）配套，取捨連同對照表寫進「待決定的取捨」第 0 條。`monsterHealth`、`monsterGold`、`bossHealthMod`（其 base 1.13、stageMult 0.005 也解出來了）三項由 `server` 改列 `table-differs`。373 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | C05 每關小怪隻數改用原版算式（2.12.9） | 上一項解出的編譯期預設值立刻派上用場。`StageLogic.GetRawMonsterCountPerStage`（RVA 0x25468a0）短到可以整段讀完：依序讀 `monsterCountInc`、`monsterCountStageDelta`、`monsterCountBase`，算 `base + 關卡 × inc ÷ (delta + 關卡)`，再交給 C# 的 `Math.Round`（以 modf 取小數後比較 ±0.5，平手進偶數），整段以 float 運算。三個欄位的編譯期預設值是 8／148／32000，於是第 1 關 8 隻、第 500 關 10 隻、第 1000 關 12 隻、第 2000 關 17 隻、第 10000 關 43 隻、第 98000 關 120 隻——引擎原本固定 10 隻，**早期偏多、後期遠遠偏少**。`tools/audit-monster-count.py` 逐指令核對讀取順序與四則運算，並直接從 `servervar-defaults.json` 取值（不重複解析，兩份記錄不會漂移），輸出 12 個關卡的樣本；引擎以 `Math.fround` 逐步模擬 float 運算並實作平手進偶數，12 個樣本逐關相同。介面同步處理：一關 120 隻時原本會畫 120 個小圓點，超過 12 隻改用進度條。登記由 `server` 改為 `default`（線上仍可覆蓋）。順帶修正 `monster-curve-evidence.json` 的措辭——原本寫「安裝包沒有帶值」，正確說法是「兩張變數表沒有帶值」，因為編譯期預設值是另一條線索。新增 `tests/monster-count.test.mjs` 6 項。371 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | R05 解出 745 個 [ServerVar] 的編譯期預設值，補上先前漏掉的第二條線索 | 專案原本的認知是「953 個 [ServerVar] 只有 28 個在包內有值」。那只對了一半：那是**變數表**的狀況，而 `ServerVarsModel` 的類別建構式還會在任何伺服器回應之前，把編譯期預設值寫進靜態區塊——劍術大師的 `costBase`／`costGrowth` 一直就是從這裡來的，只是沒有人把它整批解出來。`tools/audit-servervar-defaults.py` 走完整個建構式（0x2618 位元組），追蹤哪一個暫存器持有 `Il2CppClass.static_fields`（偏移 0xb8），把寫進去的每一個純量記下來，處理 `mov`／`movk` 立即數、rodata 載入的 `ldr q/d/s`、`dup v.2d`、以及 scaled 與 unscaled 兩種 `str`／`stp` 形式；**954 個靜態欄位解出 745 個**（int、float、bool、double），其餘 209 個**逐項列名**而不是含混帶過（string 39、bool 63、int 57、int[] 12、float 20、GHDouble 10、double 5、TimeSpan 2、List&lt;float&gt; 1）。可信度靠三重自我檢查：本專案先前手工取得的 `helperUpgradeBase`＝1.08、`playerUpgradeCostBase`＝5、`playerUpgradeCostGrowth`＝1.075，走這條路得到同一個數字（不符就中止）。與變數表互證：`hoursToCollectEgg` 兩邊都是 4、`clanNameChangeCost` 兩邊都是 800；而 `maxStage` 編譯期預設 1,000,000、被變數表覆寫成 98,000，正好示範**預設值不是線上值**。新增 `tests/servervar-defaults.test.mjs` 5 項（含引擎常數必須等於解出的值、覆蓋案例）。ROADMAP 的「伺服器參數的證據界線」一節改寫為兩條線索並說明其分別。**這批值本身還沒有接進任何公式**，接的時候一律登記為 `default`。365 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | R05 英雄升級費用改用 8.2 的原生成長率（2.12.8） | 引擎的 1.075 是 7.5 基準，`HelperInfo.json` 也只有 `PurchaseCost1` 一欄，看起來像「包內查不到」。實際上成長率是 [ServerVar] `ServerVarsModel.helperUpgradeBase`，而它和劍術大師的 `costBase`／`costGrowth` 一樣**在程式裡有靜態預設值**：`tools/audit-helper-cost.py` 從 `ServerVarsModel..cctor` 解出唯一一次寫入，值為 float 的 **1.08**（位元 0x3f8a3d71）。它確實是費用公比而不是同名的別的東西——`HelperInfo..ctor` 讀它之後取對數存進 `helperUpgradeBaseLog`、把 base−1 存進 `helperUpgradeBaseMinusOne`，正是等比級數需要的兩個導出量，`GetPurchaseCost` 再以 `GHDouble.Pow` 乘上基礎費用（`GetMaxNumUpgrades` 用對數那個、`GetEvolveAdditionalCost` 用 base−1 那個）。引擎改用 `HELPER_DEFAULTS.costGrowth = Math.fround(1.08)` 保留 float 精度，算式結構不變，登記由 `baseline-75` 改為 `default`。**影響**：同一位英雄的下一級價格在 100 級約貴 1.6 倍、500 級約貴 10 倍、1000 級約貴 100 倍；已有等級與金幣不變。**沒解出來的部分照實記錄**：相鄰的 `helperUpgradeLevelTiers`／`Modulus`／`Offsets` 三個 int[] 由 `InitializeArray` 從中繼資料填充，本次未解出內容，可能對特定等級區間另有修正。新增 `tests/helper-cost.test.mjs` 5 項（含「引擎用的值連 float 精度都與原生相同」與「相鄰兩級的費用比就是公比」）。360 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | R05 英雄傷害曲線對上原生，多出來的成長率登記為不一致 | 查 `HelperInfo.GetRawDPS`（RVA 0x22491ac）：它就是 `LevelCurve.GetTotalImprovementByLevel(昇階, 等級) × 等級 × GetBaseDamage(昇階)` 三個因子相乘，完整的 `GetDPS` 在外面只再乘武器傷害、`GetAllHelperDPS` 與 `GetIndividualEnhancedMultiplier`。`tools/audit-hero-dps.py` 除了記下呼叫順序，還把兩個方法載入的每一個浮點常數列出來——**兩個方法一個浮點常數都沒有載入**，所以逐級成長率不可能藏在原生路徑裡；里程碑累計倍率本身就是整條曲線（`HelperImprovementsInfo` 1015 列、七個昇階各 145 段，Ascension 0 到 6000 級累計 1.18×10^189）。對照結果：`TT2_HERO_MILESTONES` 145 列與該表 Ascension 0 的 `PrecalculatedAmount` 三欄**逐列完全相同**，引擎與原生的唯一差異就是額外乘上的 `1.035^(等級−1)`（新測試以乾淨存檔在 1／10／50／200／1000 級驗證比值正好等於它，等於同時釘住其餘因子都正確）。**這一項沒有移除**：1.035 是 7.5 基準的產物，與同為 7.5 近似的怪物血量曲線（18 × 1.32^關卡，十個具名 [ServerVar] 在包內都沒有值）配套，單獨拿掉會讓英雄傷害在 1000 級時少 8.4×10^14 倍，等於在沒有原版難度基準的情況下只改一邊；因此在登記表由 `baseline-75` 改列 `table-differs` 並寫明理由與影響。新增 `tests/hero-dps.test.mjs` 5 項，`tests/formula-sources.test.mjs` 同步為兩處不一致。純證據與文件變更，不動遊戲數值，版本號不變。355 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | V03／V06 影分身自己攻擊，傷害數字依原生分類（2.12.7） | 先查原生再動手。`tools/audit-damage-text.py` 產出 [damage-text-evidence.json](reference/tt2/8.2.0/damage-text-evidence.json)，四件事都是程式化解析、不是手抄：一、`DamageType` 有 41 個具名來源；二、`MonsterModel.CanShowDamageText` 是一個對 damageType−1 的跳表，把 33 個來源各自對到**唯一一個** `OptionsController` 的 `<名稱>Off` 布林（劍術大師 5 個、寵物 7 個、公會飛船 7 個、匕首 3 個、金槍 3 個、天堂 3 個、影分身 2 個、特殊攻擊 3 個），其餘 7 個（英雄家族、Splash、MegaCoin）永遠顯示；三、`MonsterController.TryDamagedAnimation` 另用位遮罩 `0xff96d76b06` 決定 25 個來源才播受擊動畫（`PetCrit`、`HeavenlyStrikeCrit`、`ShadowCloneCrit` 都不播）；四、**負面結果**：`MonsterController` 有十個受擊染色常數（已解出 RGBA），但掃過該類別其餘 47 個方法後**沒有任何一個讀它們**，`StartDamagedAnimation` 也不看 DamageType，所以受擊動畫不依來源上色，這十個顏色不能當成原版行為。實作面：`ActiveSkillModel.GetCloneAttackRate` ＝ `max(1, Bonus(ShadowCloneSkillAttackRate) × Bonus(CompanionAttackRate))`，`PlayerController.ShadowCloneAttackLoop` 以 `WaitForSeconds(1 ÷ 該速率)` 為間隔，因此把影分身從 `advance()` 的每 100ms 傷害 tick 裡拆出來：改為每 1÷速率 秒獨立攻擊一次，記在新的 `cloneAt`／`cloneAttacks`／`lastCloneHit`，介面比對次數變化才冒數字（與 2.12.3 的寵物、天堂同一原則）。每秒總傷害不變（原本是一秒十次各十分之一），但技能樹「影分身」天賦本來就帶的 `ShadowCloneSkillAttackRate`（+10%～+95%）原先完全沒有作用，現在會加快攻擊。設定新增四個傷害數字開關，存在本機而非存檔。新增 `tests/damage-text.test.mjs` 12 項（證據雜湊、跳表互斥且分完、四個開關與原生欄位逐項相同、受擊動畫名單、十個染色 0 個讀取且原始碼不得使用那些色碼、速率算式、節奏與步長無關、技能結束不補打、舊存檔缺三個欄位可讀、蛻變後歸零），`tests/damage-numbers.test.mjs` 增為四種數字，`cloneAttackRate` 登記進公式來源表（native 1 項、invented 2 項、server 1 項）。350 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | V05 技能說明改用安裝包原文（2.12.6） | 上一輪把六個技能的名稱換成官方字串後，說明仍是手寫的（「提高點擊傷害」之類）。安裝包其實每個技能有三層文字，且都是乾淨的繁中、沒有夾英文或樣板殘留：`ACTIVE_SKILL_DESC_*` 是敘述、`ACTIVE_SKILL_QUICK_DESC_*` 是帶 `{0}` 佔位符的效果說明、`ACTIVESKILLSECONDARY_*` 是次要效果（六個裡有四個有）。`tools/import-skill-names.py` 擴充為同時產生 `lib/tt2-skill-text.ts`，並在匯入時就擋下未翻譯或缺佔位符的字串。介面改法：技能面板的效果那行改用官方句子、把引擎算出的倍率填進 `{0}`（例如「造成 9925.7京 倍點擊傷害」），敘述另起一行；戰鬥區技能鍵的提示同樣改用敘述。`SKILLS[i].desc` 改為讀官方敘述，手寫句子全部移除。**次要效果這次沒接**：四個技能的 `{0}` 是機率或次數，格式不同，寧可不顯示也不顯示錯的。另記一個觀察：`ACTIVE_SKILL_QUICK_DESC_CRITBOOST` 官方作「增加致命爆擊傷害 {0}x 倍」，「x」與「倍」語意重複，但它是完整繁中、沒有未譯英文，依照既有原則照抄不改。tests/name-parity.test.mjs 增為 9 項，新增兩項釘住每個技能都有官方敘述與含 `{0}` 的效果說明、四個有次要效果、以及引擎與面板都讀官方字串且舊的手寫句子不得殘留。338 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | V04 暴擊數字的裁切補完（2.12.5） | 2.12.4 上線後在線上複測，發現**修得不夠**：點擊與寵物數字已經不越界，但暴擊仍超出 19 像素。原因是邊界常數是照本機存檔的數字寬度（200 像素）估的，線上存檔關卡更高、數字更長——實測「暴擊 7.4×10^86」在 40 像素字級下寬 256 像素，比一半的手機戰鬥區還寬。改法兩道：邊界常數改用實測的 256 的一半再加餘裕（132），並在 780px 以下把傷害數字字級調小（一般 32→26、暴擊 40→30、天堂 38→28），桌面維持原大小。以線上同等規模的存檔（第 2050 關）複測：375px 手機上暴擊寬度由 256 降為 188，兩側邊緣各點 25 次，點擊／寵物／暴擊的超出量全為 0；1280px 桌面同樣全為 0（暴擊 251 像素仍在 579 像素的戰鬥區內）。336 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | V04 新玩家逐頁點測與傷害數字裁切修正（2.12.4） | 先前的實測都用重度存檔，這次清掉本機存檔以全新玩家走一遍。**面板本身沒有問題**：神器頁那 103 個可按的按鈕是「數值來源與效果」說明連結而非會落空的升級鍵；商店在 0 鑽石時四個購買鍵全部正確停用、只有每日登入可領；裝備與轉生的空狀態都寫明了出路；寵物那句「每 N 次有效點擊」是動態讀 `petRequiredTaps` 而非寫死。教學也照 2.11.0 的 51 步推進（觸摸攻擊 → 收集 30 金幣並雇用一位英雄 → 離線收益說明），招募後英雄數正確變成 1/37。**找到一個真的缺陷並修掉**：傷害數字以點擊位置為中心，在 375px 手機上點泰坦右緣時，寵物數字會超出戰鬥區右側 53 像素被 `overflow:hidden` 裁掉；點擊與暴擊數字在邊緣也有同樣問題（這是既有的，不是 2.12.3 引入的）。改法：新增 `clampFloat(x, 戰鬥區寬度)`，以最寬的暴擊數字（實測 200 像素）的一半再加餘裕為邊界，依實際寬度換算成百分比，所以桌面版的位置感幾乎不受影響、手機版才收緊。修正後在 375px 兩側邊緣各點 40 次，四種數字（點擊、暴擊、寵物、天堂）的超出量全為 0。tests/damage-numbers.test.mjs 增為 5 項，新增的一項直接以手機寬度驗證左右緣不越界、寬螢幕夾擠更寬鬆、中央不被移動、量不到寬度時不強行夾擠。336 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | V03 傷害數字分出來源（2.12.3） | 引擎本來就會算寵物攻擊與天堂聖擊的傷害，但畫面上只有點擊傷害會冒數字，這兩個流派做了什麼玩家完全看不到。改為由引擎的計數器決定要不要冒數字：點擊前後比對 `petAttacks`，動作前後比對 `heavenlyStrikes`，只有真的增加才顯示，顯示的數值分別取 `lastPetHit` 與 `buildDamage(s,'heavenly')`，都是引擎實際使用的數字而不是另外估算。三種數字各有樣式（點擊暖白、寵物淺藍帶 ⚡ 且偏移到側邊、天堂金色帶 ☄️ 加光暈）。新增 tests/damage-numbers.test.mjs（4 項）：驗證寵物確實在第 N 次點擊發動（N 等於 `petRequiredTaps`）、天堂聖擊發動才計數而冷卻中按不計數、介面確實讀計數器與引擎數值而非自行推測、三種樣式顏色不得重複。瀏覽器實測：30 次點擊觀察到 tap 22、crit 8、pet 6（每 5 次點擊一次寵物攻擊，剛好 6 次）、heavenly 1。335 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | R05 逐項查證寵物蛋、技能點與昇階費用的來源 | 延續聖物那次的做法，把登記表裡幾條寫著「安裝包未見對應欄位」的條目拿去對原生的 953 個 `[ServerVar]` 欄位名，結果三條都查得出來，而且結論不同：**（一）寵物蛋間隔查到是對的**——原生欄位 `hoursToCollectEgg`，而安裝包的 ServerVarOverride 剛好帶了值 `4`，與本專案沿用的四小時一致，這是少數包內能核對上的欄位，狀態由 baseline-75 改為 table；但同一條的「上限兩顆」對應 `maxPetEggs`（另有 `vipStatusMaxPetEggsAdditive`），包內沒有值，拆出來記為 server。**（二）技能點的發放節奏查不到**——`skillPointsStageDelta`（每幾關一點）與 `skillPointsStageMin`（起算關卡）都是具名 `[ServerVar]`，五個 skillPoints 欄位中只有 `skillPointsPrestigeMax`（180000）有值，所以「每 50 關一點、減一」由 baseline-75 改為 server。**（三）昇階費用的結構本來就錯**——引擎用的是同一個倍率連乘（1e4^次數 × 1e6），原生則是每位英雄在 HelperInfo 各有 `AscendCostExpo1–6` 六個逐階係數（H18 為 255、2216、4748.5、7281、9813.5、12346）；資料在包內、尚未接入，但即使接上仍缺 `ascendCostExpoScaling`、`maximumHeroAscension`、`ascensionMinGoldExp` 三個無值欄位。另外確認英雄里程碑的來源：`HelperImprovementsInfo`（1015 列，鍵為 Ascension＋Level，含 Amount 與 Precalculated 兩欄）正好對上原生 `LevelCurve.AddEntry(ascension, level, amount, precalculatedAmount)` 的四個參數，也就是英雄成長是查表而非單一指數，已把該表列為 heroDamage 的來源引用。登記表 50→52 項來源，baseline-75 8→6、table 15→16、server 10→13，待核實 27→28（拆項所致，不是退步）；tests/formula-sources.test.mjs 的計數同步更新。未改任何遊戲數值，版本號不變。331 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | R05 聖物公式的來源判定由「沿用 7.5」改為「伺服器供給」 | 登記表原本把蛻變聖物的 `關卡^1.7 ÷ 100` 記成 baseline-75（沿用 7.5 基準、尚未對 8.2 核實），語氣上像是「還沒查」。實際查過之後是「查不到」：`PrestigeModel.GetBonusRelicsFromStageCount`（RVA 0x23ff6cc）裡沒有任何對應的浮點常數，係數是從模型欄位載入的（`ldr d9,[x8,#0x170]`、`ldr d10,[x8,#0x1b0]`、`ldr w9,[x8,#0x214]`），而原生的 953 個 `[ServerVar]` 欄位裡正好有十個具名係數對得上這條式子——relicStageBase、relicStageBase2、relicStageExpo、relicStageExpo2、relicStageExpo3、relicStageExpoMax3、relicStageMult1–3、relicStageOffset——**安裝包一個值都沒帶**。另外記下原生還有三條本專案未實作的聖物乘數（累加、季節、新手），31 個含 relic 的伺服器變數中只有 additiveRelicMultiplierMax（150000）與 relicsStageMax（180000）有值。這與怪物血量／金幣曲線是同一類結論：資料表看得到、決定曲線的係數看不到。登記表 49→50 項來源，server 8→10、baseline-75 9→8，待核實 26→27；tests/formula-sources.test.mjs 的計數同步更新（那幾個數字本來就是為了讓狀態變動看得見而釘住的）。未改任何遊戲數值，版本號不變。331 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | V07 大型列表（2.12.2） | 上一輪查明的先決條件這次補上了：`<GameContent>` 的元素每格都被重新建立，React 不會因為 props 相同而跳過，所以只降低面板快照頻率不會少做事（當時 A／B 建置量不出差異）。這次三件事一起做：面板包成 `React.memo`、`act` 與 `onPrestige` 改用 `useCallback` 取得穩定參照、面板改讀自己的快照（每 300 毫秒，或玩家動作時立刻更新）。**量測方式改用可讀函式名的未壓縮建置加 CDP Profiler**，比先前的主執行緒佔用取樣穩定得多：重度存檔（關卡 1200、英雄 800 級、60 件神器、天賦全滿、背包 100 件）停在裝備分頁，各跑三次 5 秒取樣——無 memo 對照組 JS 時間 140／156／156 毫秒、React 重繪 55／56／56 毫秒；本次 95／108／94 毫秒、18／23／32 毫秒。兩組完全不重疊，中位數 JS 156→95（−39%）、React 56→23（−59%）。代價是「等金幣攢夠」時按鈕最多慢 0.2 秒亮起，玩家自己按的動作一律立刻反映（瀏覽器實測：升級後 60 毫秒內畫面已更新）。tests/ui-cadence.test.mjs 增為 9 項，新增五項釘住間隔是整數倍、到期判斷、除點擊外每個動作都立刻更新、**面板必須真的被 memo 包起來且三個 prop 都穩定**（否則這次的改動又會白做），以及迴圈與前景回復的更新路徑。331 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | V05 技能與天賦名稱逐項對照（2.12.1） | 六個主動技能的名稱原本是寫死在 `lib/engine.ts` 的字串陣列，與安裝包逐字比對後發現其中一個已經飄掉一個字：官方是 `ACTIVE_SKILL_NAME_HELPERBOOST`＝「戰爭狂**吼**」，專案寫「戰爭狂**嚎**」。改為從 `SOURCE_NAMES` 讀官方字串，其餘五個本來就相同。66 個天賦則是 62 個與官方 `SKILLTREE_*` 完全相同，另 4 個的官方繁中本身夾著英文（Ember藝術／Voltaic Sails.／Chercerson香火／Midas溢出，英文原文為 Ember Arts／Voltaic Sails／Chesterson Incense／Midas Overflow），依既有的「不直接照搬錯譯」原則維持本專案的譯法（餘燼之術／雷電船帆／寶箱泰坦薰香／米達斯滿溢）。新增 `tools/import-skill-names.py` 與 skill-name-parity.json 記錄包內狀態，並把 `ACTIVE_SKILL_NAME_` 納入 `tools/import-apk-reference.py`。**沿路修掉匯入工具的一個實質缺陷**：14 個 `BACKGROUND_NAME_*` 當初是在工具外手動加進 SOURCE_NAMES 的，重跑匯入會把它們整批刪掉；已改為從 BackgroundInfo 取主題清單一併產生，重跑後名稱數由 404 增為 410（多出六個技能）。同一個名字也出現在三個效果標籤與一個技能對照表裡，一併改成官方寫法，避免介面一邊寫「吼」一邊寫「嚎」；bonus-label-parity.json 隨之重新產生（差異數仍為 90，只是本專案側的那一列更新）。tests/name-parity.test.mjs 增為 7 項，新增的三項釘住技能名稱必須來自官方字串、被標記的天賦不得照抄含英文的字串且自己的譯名不得含英文、以及 SOURCE_NAMES 內不得混入任何含英文或樣板的值。326 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-17 | V03 怪物輪替與頭目改用原版規則（2.12.0） | 原本每一關出現哪隻怪是自訂的 `((關卡−1)×6＋擊殺數) % 60`，頭目就是同一隻小怪換色。安裝包其實把這件事寫死在兩張表裡：BackgroundInfo 的 70 個關卡列各有主題、頭目與 10 隻小怪，BackgroundCycleInfo 的 48 段各有 ThemeEnd、MonsterDiversity 與 Scale。實作前先逐一反組譯確認取法，新增 `tools/audit-stage-monsters.py` 與 stage-monster-evidence.json 釘住 8 個方法的位址與位元組雜湊：`GetCycleIndex` 以 BinarySearch 找段並在完全命中時退一格（所以**關卡 25 仍屬第一段**）；`GetBackgroundIndex` 算 `(關卡 − 段鍵 − (段索引>0?1:0)) mod ThemeEnd`，也就是**每一段都從關卡列 1 重新走、走滿 ThemeEnd 列**；`GetLevelInfo` 直接取用不取模；`LoadMonsterByStageLevel` 只載入該列的前 MonsterDiversity 隻；`SpawnMonster` 在載入清單長度不足時記錯誤，否則 `Random.Range(0,count)` 取索引，**均勻隨機**。**因此發現既有實作有一處實質錯誤**：主題並不是從第 1 關就走完 14 個，第一段只走前 25 關（5 個區域），要到第 406 關才會 70 列全開；原本的世界地圖一開始就列出全部 14 個區域。改法：新增 `tools/import-stage-monsters.py` 產生 `lib/tt2-stages-data.ts`（70 列、48 段、141 個怪物識別字），`lib/tt2-stages.ts` 實作上述規則，`lib/tt2-themes.ts` 的 `themeIndex`／`themeStageRange` 改為跟著同一條規則（匯出未變）。141 個識別字要共用本專案自製的 60 個精靈，改以貪婪著色指派：**同一關卡列不會有兩隻怪長一樣，而且 60 個精靈全部用得到**（先前用等差對應只能做到其中一項）。引擎在 `spawn()` 時用存檔內的 rng 選定並存進 `State.monster`，所以畫面不會每格重擲；`monsterIndex()` 保持純函數（在 render 內呼叫，不能消耗 rng），舊存檔與遷移路徑都會補上該欄位。新增 tests/stage-monsters.test.mjs（11 項）：資料與參考表逐格相同、段邊界（25 屬第一段、26 起新段）、每段從列 1 走滿 ThemeEnd、4000 關內索引不越界、載入隻數與縮放只增不減、頭目取自該列（關卡 1 與 3 小怪清單相同但頭目不同）、精靈指派完整無同列衝突、隨機挑選落在清單內且兩端不越界、地圖只列出該段到得了的區域、以及引擎存下選擇後重複讀取不變。另更新 tests/themes.test.mjs 兩項改釘新規則。瀏覽器實測：第 3 關世界地圖列 5 個區域（約特王國 1—5 至隱密城市 21—25）、精靈寬度 195px（0.75 倍）；從第 3 關推到第 21 關期間出現 10 種小怪與 7 種頭目，其中 2 種頭目同時也在小怪清單內（與資料相符）；第 2059 關地圖列 14 個區域（2016—2085）、寬度 325px（1.25 倍）。323 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 與靜態建置無誤。 |
| 2026-09-16 | V04 補上分頁圖示（2.11.5） | 線上實測音效時發現主控台唯一的錯誤是 `favicon.ico` 404：`public/favicon.svg` 一直隨站發布，但 `static/index.html` 沒有 `<link rel="icon">`，瀏覽器只好依慣例去要網域根目錄的 `favicon.ico`，那個路徑在 Pages 的 base path 之外，必定 404。改用相對路徑 `href="favicon.svg"`，在本機 base `/` 與 Pages base `/titan-echo/` 下都解析正確。本機實測：圖示成功載入 712 位元組，favicon.ico 請求數為 0。 |
| 2026-09-16 | V06 音效、背景音樂與音量設定（2.11.4） | 原本的聲音只有一個三角波嗶聲（點擊與暴擊兩種音高）、一個不會保存的開關。安裝包的音訊不是本專案的素材、不進版控，因此改為在瀏覽器即時合成，並把「聲音是什麼」寫成資料而非程式：新增 `lib/audio.ts`，一個音效就是一個振盪器沿著 `notes` 逐音滑音、每音 `step` 秒、`gain` 是進混音前的自身音量，最終音量＝總音量 × 該聲道音量 × 自身音量。九種聲音：點擊、暴擊、施放技能、升級與購買、掉落與妖精、頭目出現、頭目擊破、蛻變，以及 A 小調五聲音階緩慢遊走的背景音樂（每音 1.6 秒、音量 0.22，設計成墊在戰鬥底下）。WebAudio 的接觸面全部收在 `app/sound.ts`，其餘都是可在 Node 測的純資料與算式。接線方式：點擊在 `strike()` 依暴擊分音，其餘 11 種動作由一張動作對照表決定（測試會核對每個動作型別確實存在於引擎的 `ACTION_TYPES`、每個音效確實存在），關卡推進、頭目出現與裝備掉落則由每格比對計數器觸發，且掛在「可見才執行」那段之後，背景分頁不出聲。設定面板新增總音量／戰鬥音效／背景音樂三段滑桿，存在 localStorage（裝置偏好，不進存檔，因為存檔格式與雲端試算表共用）；**預設三段皆為靜音**，沒被要求前不出聲，舊的布林開關值也讀得懂。新增 tests/audio.test.mjs（9 項）：釘住每個音效的波形／音高範圍／長度／音量合法、戰鬥音效不超過 0.7 秒、驗收清單上的每種聲音都有對應音效、音量相乘且任一段為零即靜音、預設靜音、壞值與舊布林值的正規化、動作對照表只指向存在的動作與音效、背景分頁不出聲，以及**每個音效都必須在 ASSETS.md 有紀錄**（V06 要求每項資源有來源）。瀏覽器實測：靜音時連點三次產生 0 個振盪器；開啟後四次點擊得到 4 個、一次技能得到 1 個；背景音樂調到 40 後立刻起音、5 秒內再 3 個音（符合 1.6 秒一音），調回 0 後 4 秒內 0 個音；三段音量都正確寫進 localStorage。沿路修掉一個實際缺陷：`.modal label{display:block}` 的權重高於單一類別選擇器，音量列會被拆成三行，改用 `.modal .volume-row` 後回到一行。312 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 與靜態建置無誤。 |
| 2026-09-16 | V02 界定小遊戲可還原的範圍 | 原本要從釣魚大賽開始做：它的資料在包內最完整（50 條魚含尺寸區間與稀有度尺寸、12 級稀有度權重、5 檔稀有度 XP、7 項升級的費用與加成、41 筆個人貢獻獎勵），而且**三個相關伺服器變數在包內就有值**（`fishingMinigameTackleboxCollectTime` 30、`fishingMinigameTackleboxBaseSize` 5、`fishingMinigamePrestigeRewardLimit` 100），在第二階段那種「一個值都沒有」的處境裡相當罕見。實作前先查原生，結論推翻了這個計畫：`FishingDerbyMinigameModel` 有 `PullFishFromServer(int numLuresSpent, string fishingMeter, …)` 與 `TryParsePulledFish(ServerResponse, out List<FishingDerbyMinigameFishInfo>, out int xp)`——**釣到哪條魚、多大、給多少 XP 是伺服器回應**，包內的稀有度權重是機率說明面板（`FishingDerbyMinigameRateInfoPanel`）的顯示資料。逐一檢查其餘五個模式後發現這是通例：狩獵 `PullBeasts`／`TryParsePulledBeasts`、挖掘 `RevealTile`（翻開單一格子就有 `ServerResponse` 回呼）與 `OnBoardComplete`、落球 `Submit(int prestigeCurrencyToSpend, List<object> ballInfos)`、迷宮 `OpenDoor(int doorIndex, Action<ServerResponse>)`、公會保險庫 `CollectRewards`；連落球的釘子與桶子數值（`ParsePegsAndBuckets`）和迷宮地圖（`ParseMaze`）都是伺服器字典解析出來的，`GetPegValue`／`GetBucketMult` 只是讀取解析結果，不是包內可確定的公式。新增 `tools/audit-minigame-boundary.py`：先比對安裝包與原生程式庫雜湊，再從符號表逐一核對 6 個模型的指定方法名稱，分成 16 個交還結果、25 個由伺服器供給、38 個客戶端自算，各附 RVA 與方法位元組雜湊，產出 minigame-server-evidence.json；方法一旦改名或消失就會直接失敗。新增 tests/minigame-boundary.test.mjs（6 項），除了核對證據與釘住的安裝包雜湊相符，還釘住**「引擎不得長出自訂的小遊戲抽選」**——`lib/` 不得出現小遊戲模組、`lib/engine.ts` 不得出現這些模式名稱，日後要實作必須是刻意改掉這條測試。V02 因此改為待證據，未寫入任何遊戲邏輯，版本號不變。 |
| 2026-09-16 | V07 加成快取鑰匙與背景節流（2.11.3） | 先用瀏覽器 CPU profile（未壓縮建置，函式名可讀）在重度存檔上定位熱點，五秒取樣中最貴的三項是 `fullSignature` 248ms、`stateEffect` 181ms、`baseSignature` 176ms —— **2.11.1 加的快取鑰匙本身變成最大成本**。原因有二：一、鑰匙是把 103 件神器、66 個天賦、套裝、附魔、30 隻寵物、5 個裝備欄與背包每件的三個欄位全部 `join` 成字串，背包裝滿時光是背包就有 300 個數字，而且**每呼叫一次 `effect()` 就重建一次**；二、一次 `effect()` 會經由 `petEffect`→`petBonus`（30 隻寵物各 2 次）與 `equipmentEffect` 再呼叫 `baseEffect` 數十次，每次又重建一次基礎鑰匙。改法：鑰匙改成「和上次讀到的數值逐格比對」——由單一 `visitBase`／`visitExtra` 走訪欄位，相同就不配置任何東西、不重算，不同就就地更新並清掉該層快取；長度改變（丟裝備、附魔、收齊套裝）由長度比對抓到；完整比對數值而不是雜湊，沒有碰撞風險。另外把已驗證的快取層往下傳，整趟只檢查一次欄位；`reaches()` 走的是靜態展開圖，改為記憶化。**新增可重跑的 `tools/bench-engine.mjs`**（Node 量測，不受瀏覽器負載影響），同一支工具在同一台機器上對照改前改後：dps() 0.577→0.217–0.336 毫秒、tapDamage() 0.456→0.137–0.192、advance() 100ms 1.355→0.440–0.555、一次點擊 1.329→0.483–0.549，約 2.4–2.9 倍；背包 0→100 件對 dps() 的增幅由 +90%（0.251→0.477）降為 +57%（0.176→0.276）。另外把畫面更新與模擬分開：分頁隱藏時 `advance()` 照常每 100 毫秒推進、只跳過 `setS()`，因為 `advance()` 對超過 30 秒的空檔會改走粗略的離線結算，停掉模擬會讓背景掛機收益變差；回到前景時立刻補一次畫面。tests/effect-cache.test.mjs 增為 9 項，新增的四項涵蓋「基礎欄位改變時走 `effect()` 也要動」「陣列變長變短」「同樣數字換位置」「面板直接呼叫的 `equipmentEffect`／`petBonus`」；已實測把版本連動拿掉後其中兩項會失敗，確認擋得住退化。tests/ui-cadence.test.mjs（4 項）釘住只有 hidden 不重繪、可見性判斷必須在模擬之後，以及「一秒一次的推進結果等同十次 100 毫秒」。297 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 與靜態建置無誤，瀏覽器實測連續升級四次數值正確、面板即時反映。**未達成的部分**：原本同時試了「升級面板改用較慢的快照頻率」來處理大型列表，做了只差該常數的 A／B 建置量測，兩邊沒有可辨識的差異（主執行緒佔用 2.1–2.9 秒／3 秒，機器負載造成的擺盪比效果還大）。檢查後確認原因：`<GameContent>` 的元素在每格都被重新建立，React 不會因為 props 相同而跳過，降低快照頻率並不會少做事，已把這條退回並記在 V07 的先決條件。另外此環境的無頭瀏覽器背景分頁始終回報 `visible`（`visibilitychange` 未觸發），背景節流只由單元測試與程式路徑確認，未取得瀏覽器端觀測。 |
| 2026-09-16 | V07 首次載入的圖片（2.11.2） | 線上版實測首次載入下載 **5,114,819 位元組、10 個請求**，其中 4,915,202 是圖片：六張圖在 1,195–1,214 毫秒同時發出、2,153 毫秒才全部到齊。定位到兩個原因：一、`swordsman.png` 972,366 位元組是唯一沒做 WebP 衍生檔的圖，卻是首載最大的單一檔案；二、掛載時的預載把五張怪物圖集全部抓下來（3,468,186 位元組），但戰鬥畫面一次只畫一隻怪、只會用到其中一張，其餘四張要等玩家推進好幾關才用得到，卻在搶畫面上那張的頻寬。改法：以同一組 Pillow 參數（quality 85、method 6）產出 `swordsman.webp`（972,366→159,518 位元組，1254×1254 RGBA 不變；合成到深色背景後與原圖 RMS 差 1.7、最大單點差 31，157 萬個像素中只有 32 個差超過 24）；把圖集檔名規則從 JSX 移到 `lib/content.ts` 的 `monsterSheet()` 與 `sheetWarmOrder()`，掛載時只暖畫面上那張，其餘依「戰鬥接下來會用到的先後」在 `requestIdleCallback` 排程逐張補上。本機實測建置後的靜態版：畫面用得到的三張（劍士、世界圖集、當前怪物圖集）在 484–507 毫秒抓完共 1,178,128 位元組，其餘四張從 987 毫秒才開始、逐張錯開，首次內容繪製 768 毫秒。另外 `.battle-bg` 還留著 `url('/battlefield.png')` —— 該元素同時帶 `world-atlas` 類別、背景圖由行內樣式覆蓋，所以這張 2.9MB 從來沒被請求過，而且該絕對路徑在 Pages 的 base path 下本來就會落空，已改成純底色。九張原始 PNG（23MB）移到不隨網站發布的 `art/`，`public/` 只留實際會被請求的 WebP，發布產物由 28MB 降到 4.9MB。新增 tests/image-loading.test.mjs（6 項）：釘住每隻怪都指向存在的圖集、預載順序第一張必須是畫面上那張且第二張是下一隻要用的、`game.tsx` 不得再寫死圖集清單、程式請求的圖片只能是 WebP 且必須存在，以及 `public/` 的檔案集合必須等於「程式會請求的檔案＋favicon」，讓原始 PNG 不會再被放回發布目錄。289 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 與靜態建置無誤。 |
| 2026-09-16 | V07 戰鬥計算熱點（2.11.1） | 實測第 1200 關、英雄 800 級、60 件神器的狀態：**dps() 一次要 4.0 毫秒**，畫面每 100ms 的 advance() 要 3.9 毫秒，一次點擊 4.4 毫秒——以每秒十次更新來說過重。定位到兩個原因：一、heroDps 對每位英雄各自解析 HelperWeaponBoost、對應流派與空間加成，三十七位英雄就呼叫 stateEffect 一百一十一次；二、effect() 每次都要走過 103 件神器、天賦樹、全部套裝、30 隻寵物與已裝備物品，而只有 baseEffect 有快取。改法：rawHeroDps 先把六個共用加成解析一次再傳給每位英雄（heroDps 多一個選用參數，公開行為不變）；effect() 整體結果依「所有走訪欄位」的簽章快取（神器、天賦、套裝、附魔、寵物等級、出戰寵物、已裝備欄位、背包每件的 id/定義/等級）。結果：dps 4.0→0.28 毫秒（14 倍）、advance 3.9→0.49 毫秒（8 倍）、點擊 4.4→0.68 毫秒。新增 tests/effect-cache.test.mjs（5 項）：逐一改動神器、天賦、套裝、寵物等級、出戰寵物、裝備等級與卸裝，每項都必須讓結果跟著變，並比對「用過快取的狀態」與「全新狀態」算出相同數值，避免日後新增相依欄位卻忘了放進簽章。283 項 Node 測試＋23 項 Python 測試通過。已部署 2.11.1 並在瀏覽器量測畫面更新間隔：寫入第 1200 關、英雄 800 級、60 件神器、天賦全滿的存檔後，241 個畫面的中位數 16.7 毫秒、95 百分位 17.3 毫秒、最差 23.5 毫秒，沒有任何一格超過 50 毫秒，主控台無錯誤，即維持穩定的每秒六十格。 |
| 2026-09-16 | L03 泰坦靈魂可還原範圍的界定 | 盤點六張表：TitanResearchInfo 327 個研究節點帶前置（RequiredResearchID）、每級費用、MaxLevel、33 種 BonusType 與 BaseEffect／EffectPerLevel／GrowthExpo；TitanCardInfo 120 張卡帶稀有度、BonusType、MaxLevel 200 與第 1／10／25／100 級的效果值；SummonLevelTitanCardRateInfo 有 1000 個召喚等級 × 5 稀有度的權重與百分比；TitanSummonLevelCost 有 3000 級的累計召喚次數；TitanSummonBannerInfo 有 7 個卡池。**缺的是收入側**：研究樹本身把 TitanSoulsChance、TitanSoulsPerPrestige、TitanSoulsPerDay 當成加成型別，代表另有基礎值，但原生 ServerVarsModel 裡與靈魂／召喚／研究相關的欄位只有 enableTitanResearchTutorial 與 titanCardInstantSummonUnlockLevel 兩個，且**安裝包兩個都沒帶值**；一次召喚要花多少靈魂也沒有對應資料表。結論：效果曲線、抽取機率與費用表可照資料表還原，靈魂與研究點如何獲得屬待證據，不能自行編造收入率。 |
| 2026-09-16 | L02 商店可還原範圍的界定 | ShopDisplayInfo 有 18 種寶箱，帶 chestID、chestType、cost（鑽石）、UnlockByStage、totalItems 與 20 個內容欄位；欄名逐一對應原生 ShopChestContentType 列舉（21 個成員）。欄位數字的意義已確認是**顯示用的件數**：原生 ShopChestInfoListItemScript.Init 的簽章是 Init(ShopChestContentType contentType, int itemCount, int chestLevel, PromotionInfo promotion)，各欄直接進 itemCount；totalItems 與逐欄加總不相等（例如 Bronze1 是 43 對 61），所以它是另外顯示的總數而非加總。**但開箱結果不在安裝包裡**：四個商店 JSON 先前已判定為伺服器回應樣本（shop-payload-samples.json 只記形狀），抽取權重與實際發放由伺服器決定。結論：商品清單（價格、解鎖關卡、廣告內容）可照資料表還原，購買後的實際獲得則屬待證據，不能自行編造抽取規則。此列狀態改為「待做（部分待證據）」。 |
| 2026-09-16 | V05 效果標籤逐項對照官方字串 | 本專案的 103 個效果標籤全部都有安裝包的 BONUS_ 官方繁中字串，無一缺漏、也無一含英文或樣板，但**其中 90 個與現用譯法不同**。沒有照抄，因為安裝包自己的繁中並不一致：逐字統計五組常見用詞後，「聖物／遺物」在 BONUS_ 區是 2:6（偏遺物）、其他字串是 40:26（偏聖物），「妖精／精靈」則是 BONUS_ 區 3:1（偏妖精）、其他字串 15:33（偏精靈），兩組方向剛好相反；照抄只會把這份不一致帶進介面。另三組（金幣／黃金 13:25 與 60:124、蛻變／聲望 1:7 與 28:114、技能／法術 25:16 與 225:47）兩邊方向一致，但改動涉及全介面用語，屬使用者決定。因此新增 tools/import-bonus-labels.py、lib/tt2-bonus-labels.ts（官方字串，未接介面）與 bonus-label-parity.json（含 90 條逐項對照與五組用詞統計），介面維持原樣；日後若要改採官方用語，differing 清單就是需逐條檢視的範圍。新增 tests/bonus-labels.test.mjs（4 項，並釘住「效果標籤不得改讀官方字串」）。278 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-16 | L01 新手教學改用原版資料（2.11.0） | 反組譯 TutorialEventModel：IsObjectiveMet（RVA 0x25DB344）就是 GetCurrentProgress() ≥ ObjectiveAmount；GetCurrentProgress（0x25DB37C）依目標型別取值，**TapCount 讀模型自己的 CurrentTapCount，因此是「該步驟開始後的點擊數」而非累計點擊**，SwordMasterLevel、ReachStage 與 UnlockHelperCount（走 HelperModel.GetUnlockedHelpersCount）皆為絕對值。原生 ObjectiveType 只有這四種，本引擎四種都量得到，所以 51 步全部可實作。新增 tools/audit-tutorial-parser.py 與 tutorial-evidence.json。以 tools/import-tutorial.py 產生 lib/tt2-tutorial.ts：採**基礎變體**，因為 B 變體與其完全相同、A 變體只調高點擊次數（27 列不同），執行期採用哪一份由伺服器指派。22 個步驟帶金幣獎勵，數值是隻數，照登入獎勵的作法乘上當前關卡的普通泰坦金幣（換算方式未查證，已在登記表標為 invented）。戰鬥區顯示目前目標與進度，文字為官方繁中字串、需要填數字的步驟會填入門檻。既有存檔從第一步開始。新增 tests/tutorial.test.mjs（7 項）。**登記表的覆蓋檢查在這次確實擋下了未分類的新匯出**，補登記後才通過。274 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 與靜態建置無誤。已部署 2.11.0 並實測：存檔設在第 2 步且已點 9 次時，畫面顯示「攻擊10倍！ · 9 / 10」（門檻已填入文字），實際點一下後步驟推進到第 4 步、金幣變成 100（該步 GoldReward 20 × 第一關普通泰坦金幣 5）、每步點擊數歸零，中間的第 3 步因為「劍術大師等級 1」已達成而一併通過，符合原生「進度 ≥ 門檻」的判定；畫面無 {0} 殘留。另修掉一處防呆：鍵盤快捷鍵原本直接對 e.target 呼叫 matches，事件來源不是元素時會丟例外，已改為可選呼叫。 |
| 2026-09-16 | 找出原生的怪物血量與金幣曲線 | 反組譯 MonsterModel.GetMonsterBaseHP（RVA 0x2325968）與 GetMonsterBaseGold（0x2325ED8），兩者呼叫同一條 GetMonsterBase(stageNum, levelOff, transcendenceLevelOff, mult, base1, base2, base3, expo1–expo4)；關卡先加上 HonourModel.ActiveHonourAmount × honourStageOffset 才進曲線。**十個血量係數與十個金幣係數全部是具名 [ServerVar]**（monsterHPMult、monsterHPBase1–3、monsterHPExpo1–4、monsterHPLevelOff、monsterTransendenceHPLevelOff，金幣側同構），逐一比對 ServerVarsInfo 與 ServerVarOverride，**安裝包一個值都沒有帶**；相鄰的 honourStageOffset 倒是有值（250）。因此登記表把「18 × 1.32^關卡」與「5 × 1.27^關卡」由 baseline-75 改判為 server：原生根本不是這個形狀，目前數值只是 7.5 基準的近似。同時記下兩處尚未實作：每關小怪隻數原生由 monsterCountBase／Inc／StageDelta 決定（引擎固定 10），頭目血量另有 bossHPModBase 與 bossHPModStageMult 參與。新增 tools/audit-monster-curve.py 與 reference/tt2/8.2.0/monster-curve-evidence.json（26 個靜態欄位、兩條曲線、20 個係數的帶值狀態），以及 tests/monster-curve.test.mjs（4 項，會在安裝包開始帶值時失敗以提醒改判）。267 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-16 | R05 核心公式來源登記表與前後對照 | 新增 [docs/formula-sources.json](docs/formula-sources.json) 與產生的 [docs/formula-sources.md](docs/formula-sources.md)：26 條核心公式、44 項來源條目，各自標記 native（反組譯確認，4）、table（安裝包資料表，14）、default（原生靜態預設值，2）、baseline-75（7.5 沿用未核實，11）、invented（專案自訂，8）、server（原生為伺服器變數且安裝包未帶值，4）與 table-differs（安裝包有值但引擎未照做，1）。tools/formula-sources.mjs 會驗證每條公式的匯出仍存在、引用的來源檔確實在 reference 目錄裡、**且十個核心模組的每一個匯出都必須被登記或分類**，因此新增或改名公式不可能悄悄漏掉。**查出唯一一處安裝包與引擎不一致**：TitanScalingInfo 的 ThemeMultiplierSequence 分四段（1–5 為 2,3,4,5,8；6–39 為 4,6,8,15,24；40–59 為 8,12,16,30,48；60 起回到 2,3,4,5,8，四個來源變體在這四列完全一致），引擎對所有關卡只用第一段，6–59 關頭目血量偏低；**暫不套用**，因為選列由 MonsterModel 存的索引決定（GetCurrentScalingInfo 只做邊界檢查後取 list[index]，索引更新方式未查證），且該序列同時是 ServerVarsModel 的 [ServerVar]。另補記兩項原本沒寫下來的規則：十連購買比逐次購買便宜（每筆各自進位，實測差 5 金），以及蛻變聖物有「至少一顆」的自訂下限。後續追查頭目倍率的選列方式：MonsterModel.StageChangedPreSpawnHandler → UpdateCurrentScalingInfo → GetScalingIndex 是從目前索引往前掃的**有狀態**流程，GetCurrentScalingInfo 只對該索引做邊界檢查；換段時還會先 RemoveCurrentScalingBonuses 再用 BonusModel.ModifyBonus 套用該列的 bonusA／bonusB。換言之倍率序列只是該列的一部分，且不是關卡的純函數，因此維持不套用並記在登記表。新增 tests/formula-sources.test.mjs（5 項）與 tests/before-after.test.mjs（升級前後、施法前後、蛻變前後共 4 項）。263 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-16 | L01 每日成就改用原版資料（2.10.0） | 以同一份解析證據（AchievementModel.Initialize 讀 DailyAchievementInfo 的 Type、RewardString、IsActive、Requirement 四欄）產生 lib/tt2-achievements.ts 的 TT2_DAILY_TASKS，10 項與門檻、獎勵字串、啟用旗標全部照抄，敘述取自安裝包官方繁中字串（例如 ClickFairies「點擊妖精 {0} 次」、Prestiges「進行蛻變 {0} 次」）。**10 項中只有 4 項可誠實量測**：獲得寵物等級、獲得裝備、點擊妖精、進行蛻變，分別接到既有的開蛋／掉裝／妖精／蛻變事件，daily 區塊新增 petLevels、equipment、prestiges 三個當日計數並隨換日一起歸零（改由 newDaily() 單點重置）。UniqueArtifacts 的 IsActive 欄在安裝包就是 FALSE；WatchVideos、SoloRaidAttacks、DiamondFairies、PerkFairy、DailyEquipment 五項所依賴的系統未實作，逐項寫明原因並不可領取。獎勵字串同時列出 Diamonds、RaidTicket、HolidayCurrency、Alchemy、GemstoneCurrency，本專案只有前者與活動幣，**只發這兩種並在介面標明其餘未開放**，不自行折算。舊的自訂五項每日任務移除，daily.claimed 由索引陣列改為型別名稱，遷移時濾掉非字串殘留（該清單從未可領取）。新增 tests/daily-tasks.test.mjs（6 項）。254 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 與靜態建置無誤。已部署 2.10.0 並在瀏覽器實測：成就分頁共 32 張卡（22 成就＋10 每日），其中 6 張顯示待實作或未啟用；實際走一次轉生→確認轉生後，「進行蛻變 1 次」由「尚未完成」變成「領取」，點下後鑽石由 3 變 8（該列 Diamonds:5），卡片改為「已領取」，未開放的三種貨幣照實標示，無 NaN。 |
| 2026-09-16 | L01 成就改用原版資料（2.9.0） | 反組譯 AchievementModel.Initialize（RVA 0x211299C）確認兩張表的讀取方式：AchievementInfo 只讀 Type、Requirement、Reward 三欄，Requirement 經 GHTool.ParseStringToListGHDouble 解析成 GHDouble 清單、Reward 經 ParseStringToListInt 存進原生欄位 **diamondReward**，因此獎勵是鑽石（官方面板字串「領取已完成層級的鑽石」相符）；DailyAchievementInfo 讀 Type、RewardString、IsActive、Requirement。新增 tools/audit-achievement-parser.py（16 處指令、11 個方法、7 個字串）。原生 AchievementType 有 23 個成員，資料表只有 22 列，**ChestTokens 沒有對應列**，已記入證據檔並在介面說明。以 tools/import-achievements.py 從已進版控的 reference 記錄產生 lib/tt2-achievements.ts（22 項、110 階），Requirement 保留來源字串（CollectGold 最後一階 1e1500，唯有 2.8.0 的配對表示法才算得出來）。22 項中 20 項已接上實際事件：沿用既有的 totalKills／bossKills／taps／best／prestiges／earnedPoints／英雄與神器數，另新增 8 個累計欄位（累計金幣、寶箱泰坦、精靈之禮、天堂聖擊、暴擊、裝備收集、聖物累計、增益使用），金幣一律經 earnGold() 同時更新餘額與累計，避免兩者分歧。錦標賽與魔力瑪尼兩項所需系統未實作，列為不可領取並寫明原因。舊的自訂 24 項成就清單移除：它從未有領取流程也從未被寫入，因此遷移只把殘留陣列清成空物件，不補發也不追回任何鑽石。新增 tests/achievements.test.mjs（9 項）。248 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 與靜態建置無誤。已部署 2.9.0 並在瀏覽器實測：收藏→成就分頁列出 22 張卡、敘述為官方繁中；寫入 taps 12000／擊殺 1500／魔王 120 的存檔後，三項顯示可領取，點下第一項鑽石由 7 變 22（第 1、2 階共 15 顆），事件紀錄顯示「成就達成第 2 階，獲得 15 鑽石。」；錦標賽與魔力瑪尼兩張卡顯示待實作與原因，無 NaN。 |
| 2026-09-16 | R04 收尾：確認加成倍率不構成封頂 | 把神器全部升到各自 max（多數為 1,000,000 級）、天賦升到各自 max（3–45）、寵物 1000 級、卷軸 100、英雄 6000 級、劍術大師 12500 級、關卡 98000，逐一量 640 個加成目標：**沒有任何一個達到 1e240**，最大的是 GoldAll 約 8.45×10^150，其次 PetDamage 1.02×10^147、ShadowCloneSkillAmount 1.24×10^132，離夾擠值仍有 89 個數量級。先前以為會觸頂是因為量測時把天賦設成 99 級，而規則的天賦上限只有 3–45，`canBuyTalent` 也確實擋住。因此 `cap()` 的 1e240 是「存檔帶著規則產不出的等級時避免 Infinity」的安全網，不是玩法上的封頂，不需要再改成配對表示法。新增 tests/effect-ceiling.test.mjs（合法上限下 640 個目標全部有限且低於夾擠、超界存檔仍保持有限、倍率餵出的傷害與金幣仍是配對）。239 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-16 | R04 大數值與原版上限（2.8.0） | 金幣、怪物血量與最後一擊改存尾數／指數配對，血量、傷害、金幣、升級費用全程以配對運算，不再經過 1e240 夾擠。三個自訂上限換成安裝包記載值：關卡 1800→98000（ServerVarsInfo maxStage）、英雄等級 2000→6000（HelperImprovementsInfo）、劍術大師 2000→12500（PlayerImprovementsInfo）。第 98000 關血量指數 11817、金幣指數 10173，皆遠超 double 範圍。劍術大師里程碑表重新匯入：原本 234 列中有 46 列被截在 1e240，最高一列 9.07E+363 根本沒有 double 表示，改以來源字串保存並在使用時解析；tools/import-player-milestones.py 改讀已進版控的 reference 記錄，不再需要 work/。`power` 在 double 範圍內改用原生次方，避免對數空間的尾差被 ceil 放大成整整一元（英雄首級費用一度由 30 變 31）。**Sheets 相容性以線上格式維持**：已部署的 Apps Script 檢查 state.gold 必須是有限數字，因此 cloudCall 送出前把金幣轉成飽和數字並在旁附上 goldAmount 精確值，舊部署原樣保存該欄、讀回時由 hydrate 還原；玩家不需重新部署試算表。舊存檔載入時就地換表示法，重複載入不變動。新增 tests/amount-format.test.mjs 與 tests/tt2-limits 相關斷言，tests/big-number.test.mjs 補 6 組新運算測試。235 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 與靜態建置無誤。已部署 2.8.0 並在瀏覽器實測：先寫入舊格式存檔（gold 123456789、lastHit 3141 皆為普通數字），重新載入後就地換成配對且數值精確（lastHit → {s:3.141,e:3}），關卡、等級、聖物、英雄等級全部保留；再從第 1799 關起算，關卡順利推進到 1814，證明舊的 1800 硬上限已解除。介面顯示 1.0×10^3000 金幣、7.2×10^219 血量、1.3×10^380 每秒傷害，無 NaN 或物件字樣。 |
| 2026-09-16 | V04 手機直向點按尺寸（2.7.2） | 以 375×844 直向實測線上版，量到 8 個小於 32px 的點按目標（本機紀錄 18×18、兩個圖示鍵 19×19、本機冒險者 22×22、×1 27×24、×10／×25 33×24、×100 40×24）。先在頁面注入候選 CSS 量測確認不造成橫向捲動（scrollWidth 375、頁首 141+202 仍放得下），再寫進 app/globals.css：780px 以下頁首鍵 40×44、購買數量鍵 44×40、保存列按鈕高 40，380px 以下頁首間距 14px→6px；桌面版不受影響。句中的行內說明連結（108×16）維持原樣，屬 WCAG 2.5.8 的 inline 例外。沿路修掉更新紀錄的實際缺陷：2026-09-14 那筆一直沿用 APP_VERSION 沒改成字面版號，導致更新頁同時列出兩筆相同版號，已定為 2.6.0。新增 tests/touch-targets.test.mjs（自行解析媒體查詢確認尺寸只在手機寬度生效）與 tests/releases.test.mjs（版號唯一、與 package.json 一致、日期遞減）。222 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 與靜態建置無誤。已部署 2.7.2 並於 375px 直向複測：頁首鍵 40×44、購買鍵 44×40、保存鍵 48×40，無橫向捲動（scrollWidth 375），更新頁 13 個版號無重複。 |
| 2026-09-16 | L01 每日獎勵校正（2.7.1） | 反組譯 DailyRewardModel.ParseInfoDocs 確認每列只讀 Day、RewardType、Count 與兩種活動貨幣共 5 個儲存格，**完全不讀 Reward 字串欄**（原生結構也沒有對應欄位）；安裝包該表在第 1、5、11 天兩欄自相矛盾。據此把第 1 天的怪物金幣由 1000 隻份改為 100 隻份，其餘 13 天原本即相符。新增 tools/audit-daily-rewards-parser.py（8 處指令、6 個方法、5 個字串）可重跑。216 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤。 |
| 2026-09-16 | V03 場景主題接線（2.7.0） | 世界地圖由 10 個自訂區域、每 20 關切換，改為安裝包記載的 14 個主題、每 5 關切換；主題名稱採用官方繁中（約特王國、神聖森林等），並經 SOURCE_NAMES 匯入（總數 404）。測試逐關卡核對 70 筆 BackgroundInfo 的主題歸屬、輪迴重複與地圖顯示的關卡範圍。213 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤。線上實測 2.7.0：地圖顯示 14 個官方場景與正確關卡範圍（約特王國 1—5 至寒冷國度 66—70），當前區域標籤正確，畫面無異常值。存檔格式未變。 |
| 2026-09-16 | 線上版實際操作驗證 | 以瀏覽器實測已部署的 2.6.4：頁面載入僅有既有的 favicon 404；連續攻擊 40 次推進擊殺數並產生金幣，中文數字單位顯示正確（2240.0萬、2688.0億、215.0兆），畫面無 NaN 或 Infinity；重新載入後進度保留（點擊數 41、擊殺 3/10），存檔的 ruleset 為 tt2-7.5.0、TT2 區塊完整、heroes[33] 與 artifacts[30] 格式不變，整份存檔無任何非有限值。本輪四項引擎改動（存檔讀取、雲端驗證、效果封頂、數字顯示）在實機皆正常。 |
| 2026-09-16 | V05 名稱對照核對 | 逐項比對專案名稱與官方繁中：英雄 37、寵物 30、神器 103 共 170 個完全一致；套裝 158 個一致，另 3 個（ScrollMaster 官方繁中為「Elysian騎士」、DaggerRogue 為「克特思羅特Razorfist」、Corrupted 為模板「腐敗{0}」）官方字串本身含未譯英文或帶佔位符，依「不直接照搬錯譯」不逕行改名，改以 set-name-parity.json 記錄原因；20 個套裝官方任何語言都未命名。測試改讀已提交資料，不依賴本機研究目錄。209 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-16 | 伺服器參數缺口盤點 | 新增 tools/server-var-gaps.mjs 與盤點文件：原生 953 個 [ServerVar] 欄位中，安裝包只帶 28 個；第二階段各項（暴擊 10、劍術大師 4、英雄 22、泰坦與金幣 67、濺射跳關 3、魔力 18、裝備 26 個欄位）在包內全部沒有值。這界定了從安裝包能核實到什麼程度，相關項目維持待證據。205 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-16 | C01 暴擊證據盤點 | 查出原生暴擊參數是 playerCritChance、playerCritMult、maxCritChance、helperCanCrit 四個伺服器變數，安裝包兩張表都沒有值；唯一有原生預設的 helperCanCrit 為 false，該指令已納入核對工具（29 處指令）。現行硬編 2%／10 倍無安裝包依據，已用測試釘住；交接文件提到的 1%／11.5 在本儲存庫無來源可追溯，未採用。202 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-16 | R04 等級上限對照 | 三個硬編上限全部與安裝包矛盾：關卡 1800 對 maxStage 98,000、英雄與劍士等級 2000 對里程碑資料的 6,000 與 12,500；但實測英雄 DPS 在 4,999 級、劍士裸傷在 7,899 級就觸頂，故上限無法單獨調高，須與數值表示法一起處理。已加測試釘住這些數字。199 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-16 | R04 封頂影響量測 | 量出怪物血量在關卡 1982、金幣在 2307 觸及 1e240，而引擎硬上限 1800 關距封頂僅約 21.8 個數量級；單獨移除 1800 關上限只能多推約 180 關。此結果把 C05 的移除硬上限與 R04 的數值表示法綁在一起，安裝包 maxStage 為 98,000、目前僅達 2%。已加測試釘住這三個數字。196 項 Node 測試＋23 項 Python 測試通過。 |
| 2026-09-16 | R04 尾數／指數型別 | 新增 lib/big-number.ts，以與原生 GHDouble 對應的兩段式表示法提供加減乘除、對數空間次方、比較與儲存字串轉換；測試涵蓋 double 無法保存的 1e800 級運算、精度截斷、跨號排序與字串來回一致，錯誤輸入一律失敗。193 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤。尚未接入引擎，存檔格式未變。 |
| 2026-09-16 | R04 數字顯示與型別證據（2.6.4） | 修正顯示函式收到非有限值時印出 NaN 的問題，並逐點測試中文單位順序與科學記號退回；新增原生 GHDouble 型別證據（尾數與指數分開、無 1e240 級上限）與各上限的 ServerVar 存在狀況（maxGold 有欄位無值）。186 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤。 |
| 2026-09-16 | R04 溢位修正（2.6.3） | 修正 stateEffect 加算分支未封頂導致魔力、技能持續與冷卻算出 Infinity 並寫入存檔、使保存與雲端同步失敗的缺陷；組合後的魔力回復再次收斂。新增極大存檔測試：離線一年與各動作後皆無 Infinity／NaN，序列化通過雲端格式與容量檢查。184 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤。1e240 封頂本身仍待原生證據取代。 |
| 2026-09-16 | R03 完成 | 三項驗收條件皆有測試證據：舊 2.6 存檔只退款一次且重跑不變、8.2 新增的 6 項技能與 28 組套裝依 ID 搬移不錯置、雲端與備份存檔先寬鬆讀取補齊再嚴格檢查並保留還原前備份。179 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤。執行時資料版本仍為 7.5，切換不屬本項。 |
| 2026-09-16 | R03 TT2 區塊 ID 對照 | 把 TT2 區塊的定位陣列（神器 103、技能樹 66、寵物 30、卷軸 37）與索引清單（套裝、碎片、附魔）納入 ID 對照，並提供依 ID 搬移索引清單的函式；直接驗證 8.2 新增的 6 項技能與 28 組套裝不會錯置既有進度，沿用舊索引則會對錯。178 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤。存檔格式、玩法與版本未變。 |
| 2026-09-16 | R03 雲端舊版相容讀取（2.6.2） | 讀取與還原改為先寬鬆檢查、補齊欄位後再嚴格確認，較舊版本寫入的雲端存檔不再被判為格式錯誤；上傳與本機保存維持嚴格格式。測試涵蓋舊版短陣列可讀不可寫，以及版本、型別、數值與容量錯誤仍被擋下。173 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤，靜態建置成功。 |
| 2026-09-16 | R03 存檔重跑保護（2.6.1） | 修正讀取存檔時，規則版本正確但缺少 TT2 區塊會誤觸舊版遷移、清空技能等級與世界進度的問題；改為只補建缺少區塊、不重跑遷移。以測試釘住舊 2.6 存檔只退款一次、重複讀取不變、短陣列補齊仍符合雲端格式。171 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤，靜態建置成功。 |
| 2026-09-16 | R03 存檔槽 ID 對照 | 新增 lib/tt2-save-ids.ts，把 69 個存檔槽對應到目錄 ID 並提供依 ID 搬移的遷移函式（增刪皆記錄、格數改變預設拒絕、重跑結果不變）；量化出索引式遷移會讓 6 個技能槽中的 4 個錯位，改以 ID 搬移後全部歸位，且全部存檔槽都能對應到 8.2 目錄。166 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤。存檔格式、玩法與版本未變。 |
| 2026-09-16 | R02 在地化盤點與資源歸類 | 以鍵覆蓋方式索引 17 個在地化檔（不複製譯文），查出繁中 8 個缺鍵、2 個空值與 178 個仍為英文的鍵作為 V05 待辦；盤點改為依位元組判定格式，214 個非資料表分為五類，不匯入清單增為 3 筆並各附理由與來源事實；372 個資源全部歸類完畢、尚未處理為 0。157 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤。遊戲執行資料與版本未變。 |
| 2026-09-16 | R02 其餘玩法資料表 | 從 227 個待判別資源挑出 16 張資料表匯入（含 3,000 筆召喚成本與 1,000 筆卡片機率），引用檢查 31,758 處且 errors／unresolved 為 0；新增鍵欄位永不省略規則與原生 FairyReward 列舉證據；教學事件兩個變體納入比較（_A 有 27 列差異、_B 完全相同）。153 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤。遊戲執行資料與版本未變。 |
| 2026-09-16 | R02 節慶與全球活動資料 | 匯入節慶與全球活動 10 張表（含三欄複合鍵的目標區表），引用檢查 31,357 處且 errors／unresolved 為 0；查出節慶全球突襲頭目使用獨立部位名稱並記錄，不與突襲部位目錄混用；獎勵詞彙模組改名並涵蓋炸彈遊戲的非 RewardID 代號。148 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤。遊戲執行資料與版本未變。 |
| 2026-09-16 | R02 小遊戲與周年賽資料 | 匯入小遊戲與周年賽 24 張表，引用檢查 30,987 處且 errors／unresolved 為 0；獎勵解析新增 RewardedBonus 三段形式（BonusID 加小數比例，不記為數量）與兩個純量代號，1,334 筆小遊戲獎勵全部解析成功；活動任務改用階段配對檢查；再記錄 AvatarAnniversary10 這處來源缺口。144 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤。遊戲執行資料與版本未變。 |
| 2026-09-16 | R02 商店與寵物樂園資料 | 匯入 ShopDisplayInfo、AdChestInfo、NewPrizeInfoDoc、PetParadiseLevelInfo 共 4 張表，引用檢查 30,966 處且 errors／unresolved 為 0；判定四個商店 JSON 為回應測試樣本而非資料表並只記錄形狀（其中 ShopInfo 非嚴格 JSON）；NewPrizeInfoDoc 納入來源變體比較並修正變體欄位比較只取保留欄位。139 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤。遊戲執行資料與版本未變。 |
| 2026-09-16 | R02 挑戰賽設定與變數綁定 | 匯入 ChallengeTournamentStartingInfo 16 筆與 953 個原生 [ServerVar] 欄位名；拆解 BonusID:數值 成對欄位並驗證九欄串接、前置名稱與同名欄位一致、發現池與裝備寵物引用，引用檢查 30,883 處且 errors／unresolved 為 0；查出 9 個伺服器變數鍵在此版沒有可綁定的客戶端欄位並記錄。134 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤。遊戲執行資料與版本未變。 |
| 2026-09-16 | R02 錦標賽資料 | 匯入錦標賽 7 張表（四組複合鍵），引用檢查 30,227 處且 errors／unresolved 為 0；確認錦標賽獎勵字串使用非 RewardID 的代號詞彙（FortuneHelperWeapon、RandomLevelPet），改以專責模組記錄；逐列比對字串與數值欄位，找出 SuperTournamentRewardInfo 的 198 列分歧並雙邊保留。128 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤。遊戲執行資料與版本未變。 |
| 2026-09-16 | R02 突襲進度資料 | 匯入突襲進度 13 張表（含兩組複合鍵），引用檢查增至 30,124 且 errors／unresolved 為 0；以 10 處指令核對確認 InfoDoc 重複欄位名的最後一欄政策並保留被遮蔽儲存格；記錄第 17 季大師階級頭像與 999 前置兩處來源缺口，未自行補造。123 項 Node 測試＋23 項 Python 測試通過，tsc --noEmit 無誤。遊戲執行資料與版本未變。 |
| 2026-09-16 | R02 伺服器變數與來源變體 | 匯入 ServerVarsInfo、ServerVarOverride、ArtifactCostInfo_A、TitanScalingInfo_A／B／C 共 6 張表；以 28 處指令核對確認覆蓋表的最後有效列政策與泰坦縮放的 A／B 表選用，七組來源變體逐 ID 比對，ScheduledBonusInfo 依原生字典解析證據不匯入。117 項 Node 測試＋18 項 Python 測試通過，tsc --noEmit 無誤，核對與匯入重跑結果一致。遊戲執行資料與版本未變。 |
| 2026-09-16 | R02 外觀支援資料 | 匯入 AvatarParticleInfo 22 筆、ProfileBackgroundInfo 47 筆與 native-cosmetic-types.json；外觀 ID、解鎖類型與背景分槽全部對上原生列舉，新增 1,650 處引用檢查（總數 19,181），列舉獨有成員只作記錄。111 項 Node 測試＋12 項 Python 測試通過，tsc --noEmit 無誤，匯入重跑結果一致。遊戲執行資料與版本未變。 |
| 2026-09-14 | R01 | 已固定目標 8.2.0；重算 XAPK SHA-256、14 表差異、目前產物數量；建立 `docs/reference-baseline.json` 與 `tools/audit-reference-baseline.py`。現行遊戲保持 2.6.0，未假裝完成 8.2 遷移。 |

現有基礎成果：2.2 寵物蓄力、2.3 基礎英雄被動與名稱、2.4 戰術洞察、2.5 劍士里程碑、2.6 劍士裸傷及費用。這些是上述大型項目的部分成果，並不是整個系統的完成證明。
