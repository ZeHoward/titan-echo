# 剩餘參考資源盤點

共 372 個 TextAsset：133 張表已匯入、17 個在地化檔已索引鍵、4 個商店回應樣本已記錄形狀、1 個圖集已記錄 ID、3 個已附理由不匯入，214 個經格式判定不是資料表，其餘 0 個仍待處理。

不是資料表的內容依格式分類：binary-resource 91、bitmap-font-descriptor 4、headerless-positional-rows 103、json-document 13、single-line-character-list 3。這些不列入待做系統數。

以下依檔名分派到 ROADMAP 項目，僅作查找順序，不是格式或玩法判定。每日配送內容與線上季節選用仍待外部證據；同名不同來源變體不得直接覆寫。

由 `node tools/reference-coverage.mjs` 產生；逐資源雜湊與狀態見 reference-coverage.json。
